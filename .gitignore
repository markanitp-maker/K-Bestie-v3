# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build
dist/


# env — 절대 커밋 금지
.env
.env.local
.env.*.local

# supabase secrets 등록용 임시 env 파일 — 절대 커밋 금지
supabase/.env.secrets

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# os
.DS_Store
Thumbs.db

# ssl certs / keys
*.pem
*.key

# GCP service account keys — 절대 커밋 금지
*-b8beddefb7c2.json
k-bestie3-*.json
*service-account*.json
*sa-key*.json

# omc internal state (Claude Code tool metadata)
.omc/

.vercel

# 테스트 계정 자격정보(로그인/비번) — 로컬 전용, 절대 커밋 금지
scratch/

# 작업 큐 처리 결과물(완료/실패 아카이브·로그·보류 기록) — 로컬 진행 상태, 커밋 안 함
requests/_done/
requests/_failed/
requests/_log.md
requests/_blocked.md
requests/_dashboard.md


# Secret env files — precise rules preserving example/template files
.env
.env.local
.env.development
.env.development.local
.env.test
.env.test.local
.env.production
.env.production.local
.env.staging
.env.staging.local
.env.*
!.env.example
!.env.template

temp_prod_link*/

test-results/
playwright-report/

# 034: 김비서 브리지 런타임 상태(질문/답변/이력) — 소스코드 아님, 로컬 상태만
.claude/kim-bridge/outbox/
.claude/kim-bridge/inbox/
.claude/kim-bridge/archive/
.claude/kim-bridge/state.json

# QA & Credentials artifacts — 절대 커밋 금지
qa_session_info.json
env_dev.txt
env_preview.txt
env_prod*.txt
temp-creds*.json
.supabase_prod_tmp/
.vercel_prod_tmp/
vercel_env_k_bestie_v3_dev.txt
scratch_dev/
scratch_none/
scratch_prod/
scratch_v3/
scratch_*/
# 앵커(/) 필수 — 앵커 없는 "reports/"는 깊이 무관하게 매칭돼 app/api/parent/reports/,
# lib/reports/ 같은 동일 이름의 소스 디렉터리까지 통째로 무시된다(.vercelignore에서
# 이미 같은 이유로 /reports/로 앵커링된 전례 있음, requests/024에서 재확인).
/reports/
.claude/kim-bridge-agent-poc/
vercel_prod.log
dev_diff.sql


.env*

.vercel

# 2026-08-13: agy 전수조사로 저장소 루트에 실제 토큰/비밀번호/개인정보가
# 하드코딩된 1회성 디버그 스크립트·덤프가 다수 발견돼 추가. 앵커(/)로
# 루트 레벨만 잡는다 — scripts/ 등 정식 스크립트 디렉터리는 무관.
/scratch_*.mjs
/scratch_*.ts
/scratch_*.log
/query-db*.js
/query-dev-db*.js
/query-schema*.js
/query-rpc.js
/query-check.js
/query_stats.sql
/fix-db*.js
/patch_*.js
/run-test*.js
/*.json
!package.json
!package-lock.json
!tsconfig.json
!tsconfig.test.json
!vercel.json
!requests/**/*.json
/tmux-*.log

# 2026-08-13: 5,424개 파일짜리 레거시 프로젝트 스냅샷 백업(2026-08-03) —
# 별도 .git까지 포함된 통째 복사본이라 추적 대상이 아니다.
/candidate-release/
/worktrees/
/e2e-results/
/.vscode/

.vercel

# 작업 폴더 정리 규칙(AGENTS.md §17) — worktree는 .worktrees/ 한 곳에만 만들고
# 세션 종료 시 제거한다. 실수로 커밋되는 것을 막기 위해 무시 목록에 둔다.
.worktrees/
worktrees/
.codex-worktrees/
.claude/worktrees/

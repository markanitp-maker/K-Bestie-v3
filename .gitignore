# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build
dist/


# env — 절대 커밋 금지
.env
.env.local
.env.*.local

# supabase secrets 등록용 임시 env 파일 — 절대 커밋 금지
supabase/.env.secrets

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# os
.DS_Store
Thumbs.db

# ssl certs / keys
*.pem
*.key

# GCP service account keys — 절대 커밋 금지
*-b8beddefb7c2.json
k-bestie3-*.json
*service-account*.json
*sa-key*.json

# omc internal state (Claude Code tool metadata)
.omc/

.vercel

# 테스트 계정 자격정보(로그인/비번) — 로컬 전용, 절대 커밋 금지
scratch/

# 작업 큐 처리 결과물(완료/실패 아카이브·로그·보류 기록) — 로컬 진행 상태, 커밋 안 함
requests/_done/
requests/_failed/
requests/_log.md
requests/_blocked.md
requests/_dashboard.md


# Secret env files — precise rules preserving example/template files
.env
.env.local
.env.development
.env.development.local
.env.test
.env.test.local
.env.production
.env.production.local
.env.staging
.env.staging.local
.env.*
!.env.example
!.env.template

temp_prod_link*/

test-results/
playwright-report/

# 034: 김비서 브리지 런타임 상태(질문/답변/이력) — 소스코드 아님, 로컬 상태만
.claude/kim-bridge/outbox/
.claude/kim-bridge/inbox/
.claude/kim-bridge/archive/
.claude/kim-bridge/state.json

# QA & Credentials artifacts — 절대 커밋 금지
qa_session_info.json
env_dev.txt
env_preview.txt
env_prod*.txt
temp-creds*.json
.supabase_prod_tmp/
.vercel_prod_tmp/
vercel_env_k_bestie_v3_dev.txt
scratch_dev/
scratch_none/
scratch_prod/
scratch_v3/


.env*

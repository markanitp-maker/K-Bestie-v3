# 음성 미션 QA 보고서 (2026-07-25)

`/k-bestie-voice-mission-qa` 스킬 기준 전체 실행. **코드는 전혀 수정하지 않았다**
(QA 전용, `git status` 무변경 확인 완료). 모든 판정은 실제 실행 증거(실제
브라우저·실제 Dev DB 조회·실제 네트워크 응답)를 근거로 했다.

## QA 개요

- 테스트 시각: 2026-07-25 (KST) / 실제 실행 창: 약 17:50~18:20 UTC
- 대상 요청서: 없음(채팅 직접 지시, 전체 회귀 QA)
- Dev URL: `https://k-bestie-v3-dev.vercel.app`
- 최근 배포: `vercel ls` 확인 결과 약 1시간 전 배포(로컬 HEAD `b485343` MBTI
  리팩터링은 이번 QA와 무관하며 Dev에 배포되지 않은 상태 — 놀이 관련 커밋이라
  음성 미션 코드에 영향 없음)
- 테스트 계정: 부모 `QA테스트` / 자녀 `QA테스트(5학년)`(childId
  `cde1b847-b1d2-4378-b337-b8cf4d532b00`)

## 사전 확인 (QA 시작 전 정본)

| 항목 | 확인 결과 |
|---|---|
| 현재 미션 정책 | SKILL.md 협의된 기준(유효답변 10개, 진행률 10%/개, 질문 순환 4단계) 그대로 적용 |
| 테스트 Dev URL | `https://k-bestie-v3-dev.vercel.app` — HTTP 200 확인 |
| 기존 세션·진행률 | QA 시작 시점 기존 mission 세션 없음(사전 정리 확인) |
| 질문 20개 구성 상태 | 5학년 적용 활성 문항 63개(2026-07-25 세션 초반 마이그레이션 반영 확인) — 실제 신규 세션 시작 시 PRIMARY 10 + RESERVE 10 = 20개 정확히 배정 확인(아래 시나리오 참고) |
| 테스트 후 복구 필요 데이터 | given_name 임시 설정(2회) → 모두 null로 원복 확인. 테스트 중 생성된 mission 세션 전부 하드 삭제(FK 자식 테이블 먼저 삭제 후 부모 삭제) — 최종 0건 확인 |

## ⚠️ 핵심 발견 — [FAIL] 텍스트 입력(음성 세션 유지 상태)으로 제출한 답변이 진행률에 전혀 반영되지 않음

**요약**: 실제 로그인된 브라우저에서 미션을 시작하고(라이브 음성 세션, 텍스트
입력 폴백 사용) 답변을 제출하면, 케이(K)는 답변 내용에 정확히 맞는 자연스러운
음성 반응을 실제로 생성·재생하지만(`chat_messages`에 실제로 저장됨 확인),
`mission_question_history.answer_classification`/`answer_status`가 영구히
`null`로 남고 `mission_progress.valid_answer_count`도 `0`에서 전혀 증가하지
않는다. 즉 **몇 개의 유효한 답변을 하더라도 미션이 절대 완료되지 않는다.**

### 재현 조건 (완전히 새로운 세션으로 재확인 완료, 이전 세션 캐시 아님)

1. QA테스트(5학년)로 로그인 → `/child/missions` 이동 → "미션 시작" 클릭
2. `/api/mission/start` 응답 확인: `{"resumed":false, "sessionId":"0e7235a3-..."}`(완전 신규 세션 확인됨)
3. "텍스트로 대화하기" 버튼으로 텍스트 입력 모드 전환
4. "친구랑 놀이터에서 놀았어"(명백히 유효하고 질문과 관련된 답변) 입력 후 전송
5. 케이의 실제 음성 반응 생성(`firstTextDelta`/`turnComplete` 스트리밍 이벤트,
   TTS 오디오 재생 시작 로그까지 전부 정상 관측) — **20초 대기 후 확인**:
   - `mission_question_history` 1번 문항: `answer_status: null,
     answer_classification: null, progress_awarded: false` (변화 없음)
   - `mission_progress`: `valid_answer_count: 0, status: "IN_PROGRESS"`,
     `updated_at`이 세션 생성 시각과 동일(그 이후 단 한 번도 갱신되지 않음)

### 근거로 확인한 것 (재현 신뢰도를 높이기 위해 교차 검증)

- 위 시나리오를 **완전히 독립된 신규 세션 2회**(하나는 이름 미설정 상태,
  다른 하나는 given_name 설정 상태)에서 각각 재현 — 두 경우 모두 동일하게 FAIL.
- `chat_messages` 테이블에는 케이의 답변별 맞춤 반응이 정확하게 순서대로
  저장돼 있음을 확인(예: "친구랑 놀이터에서 놀았어" → "친구가 놀이터에서
  같이 놀아줘서 좋았겠다!") — **대화 자체(LLM 반응 생성·TTS·재생)는 완전히
  정상 동작**한다. 문제는 오직 미션 진행률/문항 상태 기록 계층에서만 발생.
- `/api/mission/answer`, `/api/mission/respond` 두 REST 엔드포인트 모두 네트워크
  레벨에서 요청 자체가 발생하지 않음(Playwright 네트워크 리스너로 전체 API
  호출 목록을 확인했으나 두 엔드포인트 모두 단 한 번도 호출되지 않음 — 20초
  이상 대기해도 지연 호출조차 없음).
- 코드 조사 결과(수정하지 않고 읽기만 함): `app/child/missions/page.tsx`의
  `handleTurnComplete` 콜백이 `/api/mission/answer`를 호출하는 코드 경로를
  갖고 있고(라인 약 420~560), Live 모드 전용 재진입 가드(라인 372~388,
  `turnPhaseRef.current !== "child_listening"`이면 조용히 `return`)가 존재한다.
  다만 이 가드가 정확히 왜/언제 걸려 실제 서비스에서도 재현되는지, 아니면
  자동화 환경(가짜 오디오 디바이스, `--use-fake-device-for-media-stream`)
  특유의 타이밍 문제인지는 **코드를 수정하지 않고서는 확정할 수 없어 이번
  QA 범위에서 결론 내리지 않는다** — 이 지점이 유력한 조사 시작점이라는
  점만 기록한다.
- **완전히 배제할 수 없는 대안 가설**: 자동화(헤드리스 + 가짜 오디오 디바이스)
  환경 자체의 한계일 가능성. 다만 대표님 실기기 재현 사례(9/10에서 멈춤,
  `requests/_done/004-mission-reserve-live-e2e.md` 참고)와 결이 비슷해 실제
  사용자 환경에서도 발생할 가능성을 배제하지 못한다 — **대표님 실기기 확인이
  반드시 필요한 최우선 항목으로 분류**.

## 시나리오별 결과

### 시나리오: 질문 풀 구성(PRIMARY 10 + RESERVE 10)
- 판정: `PASS`
- 신규 세션 시작 시 `mission_question_history`에 selected_order 1~10이
  `PRIMARY`, 11~20이 `RESERVE`로 정확히 20개 배정됨을 실제 DB 조회로 확인.

### 시나리오: 정상 유효 답변 처리
- 판정: `FAIL`
- 위 "핵심 발견" 참고. 케이의 반응 자체는 자연스럽고 정확했으나 진행률
  0%→10% 증가가 전혀 일어나지 않음.

### 시나리오: 무효 답변(거부/무관) 재질문 및 RESERVE 승격
- 판정: `BLOCKED`
- 진행률 계층이 전혀 갱신되지 않는 근본 문제로 인해, 이전 세션(추후 FK
  제약으로 실제로는 계속 같은 오래된 세션이 재사용되고 있었음을 뒤늦게
  발견해 무효화됨)에서 관찰했던 "승격" 신호는 재검증 결과 **문항의 초기
  고정 배정(1~10=PRIMARY)을 승격으로 오인한 것으로 판명** — 즉 실제 승격
  여부를 이번 QA에서 신뢰성 있게 확인하지 못했다. 근본 원인(답변 처리 자체가
  기록되지 않음)이 해소되기 전까지는 이 항목도 검증 불가.
- **중요 지적**: 이전 세션(`tests/qa-reserve-e2e.mjs`)의 "승격됨" 로그 출력도
  같은 착시일 가능성이 높다 — 해당 스크립트도 `selected_order===3`이
  `PRIMARY`인 것만 확인했는데, 이는 애초에 문항 1~10이 전부 PRIMARY로
  배정되므로 실제 승격 여부와 무관하게 항상 참이다. 이 스크립트가 과거
  "PASS"로 보고했던 회귀 테스트 결과를 신뢰도 낮음으로 재분류할 것을 권고.

### 시나리오: 진행률 10%씩 증가
- 판정: `FAIL` (핵심 발견과 동일 원인)

### 시나리오: 유효 답변 10개 완료
- 판정: `FAIL` (진행률이 아예 오르지 않으므로 완료 자체가 불가능)

### 시나리오: 이어하기(세션 재개)
- 판정: `PASS`(부분적) — 대화 메시지·질문 인덱스 복원은 정상 확인
- `data.resumed`/`data.questionStates`/`findResumeIndex` 로직이 정상 동작해
  새로고침 후에도 세션이 재사용되고 첫 질문으로 리셋되지 않음을 확인. 다만
  "유효 답변 수 유지"는 애초에 valid_answer_count가 절대 오르지 않으므로
  의미 있게 검증할 수 없었다(`BLOCKED`로 함께 표기).

### 시나리오: 질문 중복 방지
- 판정: `NOT TESTED`
- 위 근본 문제로 인해 문항이 실제로 "다음 문항"으로 전환되는지 자체를
  신뢰성 있게 검증할 신규 답변 처리 사이클을 만들 수 없었다.

### 시나리오: K 상태 표시(듣는 중/생각하는 중/말하는 중) 순서
- 판정: `NOT TESTED`
- 상태 배지(`VoiceConversationStateBadge`)는 매우 짧은 시간 표시되는 전이형
  UI라, 헤드리스 자동화의 폴링 타이밍상 안정적으로 캡처하지 못했다(DOM
  스냅샷 시점에 이미 idle로 복귀해 빈 배열만 확인됨). 실기기 육안 확인이
  필요하다.

### 시나리오: 응답 시간 측정
- 판정: `BLOCKED`(부분 데이터만 확보)
- 콘솔 타이밍 로그에서 "케이 첫 오디오 재생 시작" 이벤트와 Gemini Live의
  `firstTextDelta`/`turnComplete` 스트리밍 이벤트 타임스탬프는 실제로 확보했으나,
  "아이 발화 종료" 기준 시각을 정확히 계측할 별도 계측 지점을 이번 QA에서는
  추가하지 않아(코드 수정 없이는 어려움) 핵심 지표(발화종료→재생시작)를
  완전한 형태로 산출하지 못했다. 참고용 관측값: 텍스트 전송 클릭 시점부터
  케이의 `firstTextDelta` 도착까지 체감 약 3~7초, 그 직후 즉시 오디오 재생
  시작 로그 확인(스트리밍 특성상 TTS는 사실상 거의 지연 없이 이어짐).

### 시나리오: 연결 불안정 오탐
- 판정: `PASS`
- 여러 차례의 정상 대화 진행 동안 "연결 불안정"/"연결 다소 불안정" 문구가
  단 한 번도 표시되지 않음을 매 시나리오마다 확인(2026-07-24 세션에서 반영된
  `recordNormalTurn()` 누락 수정이 실제로 유효한 것으로 보임).

### 시나리오: 프로필 컨텍스트(개인화 인사)
- 판정: `PASS`
- `given_name`이 없는 QA 계정에 신규 세션 시작 시 "안녕~ 난 케이야. 넌 이름이
  뭐니?"(식별 질문) 정상 노출 확인.
- `given_name`을 임시로 "테스트아"로 설정한 신규(비-재개) 세션에서는
  "테스트아야! 오늘도 만나서 반가워. [실제 첫 질문]"으로 정확히 개인화되고
  식별 질문이 반복되지 않음을 확인 — 2026-07-24 세션에서 수정된 신원질문
  차단 로직(`filterIdentityQuestions`)이 실제로 유효함.
- (최초 시도에서는 세션이 실수로 "이어하기"로 잡혀 잘못된 FAIL처럼 보였으나,
  이는 아래 "QA 도구 자체의 결함"에 기인한 테스트 방법론 오류였고, 완전
  신규 세션으로 재확인해 정정함.)

### 시나리오: fallback 경로(음성→텍스트 전환)
- 판정: `PASS`
- "텍스트로 대화하기" 버튼이 항상 노출되고, 클릭 시 텍스트 입력 UI로 정상
  전환됨을 모든 테스트 런에서 확인. 다만 이 경로로 제출한 답변이 위 핵심
  발견처럼 진행률에 반영되지 않으므로, "폴백 UI 자체는 있지만 그 경로로는
  미션을 완료할 수 없다"는 점을 함께 기록한다.

## 전체 판정

- **필수 시나리오 전체 PASS 아님.** 핵심 완료 조건(유효 답변 10개 → 진행률
  → 완료)이 FAIL이므로 이번 QA는 전체 PASS로 보고할 수 없다.
- 발견된 문제:
  - **[복잡]** 텍스트 입력(라이브 세션 유지 상태)으로 제출한 답변이
    `mission_question_history`/`mission_progress`에 전혀 반영되지 않음 — 미션
    완료 불가능한 치명적 결함. 재현 조건은 위 상세 기재.
  - **[복잡]** 과거 004 회귀 테스트 스크립트(`tests/qa-reserve-e2e.mjs`)의
    "RESERVE 승격 확인" 로직이 실제로는 초기 고정 배정을 승격으로 오인하는
    착시일 가능성 — 해당 스크립트가 과거 보고한 PASS 결과의 신뢰도 재검토 필요.
  - **[단순, 참고]** `lib/questions/answer-classifier.ts`가
    `responseMimeType: "application/json"`을 사용 중 — 프로젝트 CLAUDE.md
    §5 검증 체크리스트("responseMimeType 사용 금지")와 어긋나는 것으로 보임.
    이번 QA 범위(코드 수정 금지) 밖이라 수정하지 않고 참고 사항으로만 기록.
- QA 계정 원상 복구 여부: 완료 — `given_name` null로 복원, 테스트 중 생성한
  모든 mission 세션(`chat_sessions`/`mission_progress`/
  `mission_question_history`/`chat_messages`) 하드 삭제 확인, 최종 세션 수
  0건. 코드 변경 0건(`git status` 확인).

## 응답 시간 요약 (참고용, 불완전)

| 구간 | 측정값 |
|---|---|
| 텍스트 전송 → firstTextDelta 도착 | 약 3~7초(관측치, 정밀 계측 아님) |
| firstTextDelta → 오디오 재생 시작 | 사실상 즉시(수십~수백ms) |
| **아이 발화 종료 → 음성 재생 시작(핵심 지표)** | 이번 QA에서 정밀 계측 못 함 — 실기기 별도 측정 필요 |

## [대표님 확인 필요] (최대 5개)

```text
1. 접속 위치: https://k-bestie-v3-dev.vercel.app/child/missions (실제
   스마트폰/태블릿)
2. 사용할 계정: 김서아 또는 김서현 실계정(QA 계정이 아니라 실제 대화 흐름을
   보기 위함 — 단, 이 QA 보고서 확인 후 대표님이 직접 로그인)
3. 말할 내용: 아무 질문에나 실제로 소리 내어 짧게라도 답변(예: "응 좋았어")
4. 정상적으로 보여야 할 결과: 답변 후 화면 상단 진행률 게이지가 실제로 10%
   올라가야 함
5. 문제가 생기면 캡처할 화면: 답변 직후 진행률 게이지가 그대로 0%(또는
   기존 값)에 멈춰 있는 화면 — 이번 QA에서 발견한 문제와 동일하면 캡처해서
   공유 부탁드립니다. 이미 김서현 계정에서 "9/10에서 멈춤" 실기기 재현
   사례(004 요청서)가 있어, 이번 발견과 같은 뿌리일 가능성이 있습니다.
```

K-Bestie-v3 오케스트레이션 규칙 (v7: requests 큐 + 유연한 리뷰 게이트)

> 최종 수정: 2026-07-24 변경 요지: codex 필수 게이트 해제(한도 초과 대응). 리뷰 게이트를 3단 체계로 전환 — agy 개발분은 메인 Claude가 리뷰, 메인 Claude 직접 개발분은 별도 `claude-review` 인스턴스가 검증, codex는 가용 시 추가 검증. Claude Code 직접 코딩 허용(우선순위는 agy). 테스트 계정 체계 및 폴더 스캔 규칙 현행화.

---

## 0. 절대 규칙 (하드룰 — 위반 시 즉시 중단)

- **[하드룰 1] 코딩은 agy 우선.** 1차 코드 작성·수정은 agy(안티그라비티) tmux 세션에 위임하는 것을 기본으로 한다. 단, 아래에 해당하면 Claude Code가 직접 개발할 수 있다.
    
    - 로직/알고리즘 오류, 아키텍처 결함, 여러 파일이 얽힌 설계 문제
    - agy가 동일 작업에 2회 이상 실패
    - 위임 오버헤드가 작업 자체보다 큰 소규모 수정
    - 긴급 처리가 필요한 건 Claude가 직접 개발한 경우 §4-B의 `claude-review` 검증을 반드시 거친다.
- **[하드룰 2] 리뷰 게이트 없이 완료 보고 금지.** 모든 코드 변경은 §4에 정의된 리뷰 게이트 중 해당하는 경로를 **실제로 실행**해 통과해야 한다. 리뷰 로그가 없으면 "완료"라고 보고할 수 없다.
    
- **[하드룰 3] 셀프 통과 금지.** 코드를 생성한 주체가 자기 결과물을 검증해 통과시킬 수 없다. 생성한 모델은 자기 맹점을 못 본다.
    
    - agy가 만든 코드 → 메인 Claude가 리뷰 (제3자)
    - 메인 Claude가 만든 코드 → 별도 `claude-review` 인스턴스가 리뷰 (제3자)
    - 어떤 경우에도 "만든 주체 = 통과시킨 주체"가 되어서는 안 된다.
- **[하드룰 4] Claude의 토큰 우선순위.** Claude의 토큰은 계획 수립·작업 분해·agy 지시·리뷰 판단·최종 통합에 우선 배분한다. 단순 반복 코딩에 토큰을 소모하지 않는다. 직접 코딩은 하드룰 1의 조건에 해당할 때만 한다.
    
- **[하드룰 5] 큐를 비우기 전에 턴을 끝내지 않는다.** `requests/` 에 처리 대기 지시서가 하나라도 남아 있으면 Claude는 턴을 종료하지 않는다. 한 지시서를 끝낼 때마다 반드시 `requests/` 를 다시 나열해 신규 파일을 확인한다(§1-A).
    

---

## 1-A. 요청 폴더 감시 규칙 (최우선)

### 큐 정의

- `requests/` 폴더가 작업 지시서 큐다.
- `_done/`, `_failed/` 에 없는 `.md` 파일이 처리 대상이다. (`_TEMPLATE.md`, `_log.md`, `_blocked.md` 는 제외)
- 처리 순서는 파일명 오름차순이다.

### 절대 원칙

- 한 지시서를 끝낼 때마다 **반드시 `requests/` 를 다시 나열(ls)한다.** 기억이나 캐시에 의존하지 않는다.
- 처리 대기 파일이 하나라도 남아 있으면 **턴을 종료하지 않는다.** 큐가 빌 때까지 계속 진행한다.

### 처리 절차

1. `requests/` 를 나열하고 처리 대상을 확정한다.
2. 가장 앞선 파일을 읽고, 그 내용을 **§2 표준 작업 흐름** 에 투입한다.
3. **설정된 리뷰 게이트 통과 후** 해당 파일을 `requests/_done/` 으로 이동하고 `_log.md` 에 [일시 / 파일명 / 개발주체 / 리뷰주체 / 변경파일 / 결과] 를 기록한다.
4. 실패 시 `requests/_failed/` 로 이동하고 `_log.md` 에 사유를 기록한다.
5. 1번으로 돌아간다. 작업 중 새로 생성된 파일도 이 시점에 발견되어 처리된다.
6. 큐가 비면 §15 김비서 보고를 실행한 뒤 종료한다.

### 큐 항목의 병렬 처리

- 파일 범위가 겹치지 않는 지시서가 2개 이상 대기 중이면 **여러 agy tmux 세션을 동시 실행**한다. 최대 3개. (§13과 동일 원칙)
- 공유 파일(라우터 인덱스 / 공통 타입 / 설정파일 / package.json)을 건드리는 지시서는 단독으로 순차 처리한다.
- 판단이 애매하면 병렬로 묶지 말고 순차로 처리한다.
- 병렬로 처리했더라도 리뷰는 **묶음 단위로 한 번** 올린다(§2 기능 단위 게이트 원칙 유지).

### 작업 규칙

- 지시서 착수 전 `docs/conventions.md` 를 먼저 읽고 그 규약을 따른다. agy 지시문에도 이 규약을 명시해 전달한다.
- 각 지시서는 `## 범위` 에 명시된 경로 안에서만 파일을 생성·수정한다.
- 공유 파일은 지시서의 `## 공유파일 수정` 에 명시된 경우에만 수정한다.
- 판단이 필요한 지점은 임의로 결정하지 않는다. `requests/_blocked.md` 에 [파일명 / 질문 / 보류 지점] 을 기록하고, 그 부분만 건너뛴 뒤 나머지를 완료한다.

### 긴급 요청 처리

- 채팅으로 새 지시가 들어오면 해당 지시를 최우선으로 처리한다.
- 완료 후 `requests/` 를 다시 나열하고, 대기 파일이 있으면 이어서 진행한다.
- 중단되었던 지시서를 재개할 때는 처음부터 다시 하지 않는다. 대상 파일의 현재 상태를 먼저 읽고 이미 완료된 부분은 건너뛴다.

### 보고

- 각 지시서 처리 후 [파일명 / 개발주체 / 리뷰주체 / 변경 파일 목록 / 상태] 를 한 줄로 출력한다.
- 큐가 비면 처리 결과 전체를 표로 출력하고, §15 김비서 보고를 실행한다.

---

## 1-B. 역할 분담

- **agy (안티그라비티 / Gemini 3.1 Pro (High)) = 1차 코딩 주체.** 신규 작성, 기능 추가, 대량 수정, 리팩터링, 단순 작업 — 코딩은 agy가 먼저 한다. 설정: `~/.gemini/antigravity-cli/settings.json` (model: `Gemini 3.1 Pro (High)`).
- **메인 Claude Code = 지휘자 + 리뷰어 + 어려운 문제 해결사 + 큐 운영자.** 전체 오케스트레이션과 `requests/` 큐 운영을 담당한다. agy 결과물의 1차 리뷰를 수행하며, 하드룰 1의 조건에 해당하면 직접 개발한다.
- **claude-review (별도 Claude Code 인스턴스) = 메인 Claude 개발분 검증 게이트 (읽기 전용).** 메인 Claude가 직접 작성·수정한 코드만 검증한다. 직접 파일 수정 금지.
- **codex = 보조 검증 (읽기 전용, 가용 시에만).** 현재 사용 한도 초과 상태. **필수 게이트가 아니다.** 가용할 때 추가 검증으로만 사용한다. codex 미가용이 큐 진행을 막는 사유가 될 수 없다.

**모델 라우팅:** 설계/계획 = Opus, 오케스트레이션/지휘/리뷰 = Sonnet, 1차 코드 실행 = Gemini 3.1 Pro (High).

---

## 2. 표준 작업 흐름 (기능 단위 리뷰 게이트)

**기본 원칙: 매 수정마다 리뷰를 부르지 않는다.** 관련 기능 묶음(또는 큐 지시서 1건) 단위로 작업을 완료하고 **자체 테스트까지 마친 뒤**, 그 묶음 전체를 한 번에 리뷰에 올린다. 같은 묶음 안에서 나오는 타입 오류·import 누락·UI 미세 조정·단순 버그 수정은 개별 리뷰 없이 모아서 처리한 뒤 묶음 종료 시점에 합친다.

**예외 — 아래 5종은 묶어두지 않고 발생 즉시 리뷰 대상으로 올린다:**

- 아키텍처 변경
- 보안 관련 변경
- DB 스키마 변경
- 데이터 손실 가능성이 있는 변경
- 다른 모듈에 영향이 큰 변경

```
① 개발 주체 결정
   ├─ 기본 → agy tmux 세션 위임 (§3, 동적 timeout)
   └─ 하드룰 1 조건 해당 → 메인 Claude 직접 개발

② 자체 테스트 (기능 묶음 완료 후)

③ 리뷰 게이트 (§4, 필수)
   ├─ agy 개발분      → 메인 Claude 리뷰
   ├─ 메인 Claude 개발분 → claude-review 인스턴스 검증 (tmux)
   └─ codex 가용 시    → 위 리뷰 통과분에 추가 검증 1회 (선택)

④ 결과에 따라 분기
   ├─ [통과] → 완료.
   ├─ [단순 문제] → 개발 주체에게 되돌려 재수정 (타입에러/오타/null 누락/import 등).
   │                재수정 후 다시 ③으로.
   └─ [복잡한 문제] → 메인 Claude가 그 부분만 직접 수정.
                     → 이 경우 리뷰 주체가 claude-review 로 전환된다 (하드룰 3).

⑤ 재검증-수정 루프는 최대 2회. 그래도 안 되면 §15로 대표 보고.

⑥ 모든 기능 묶음을 병합한 뒤 → 전체 통합 리뷰 게이트(필수, 생략 불가)
   └─ 통합 리뷰는 claude-review 인스턴스가 수행한다. codex 가용 시 병행.

⑦ 완료 → 큐 항목이면 §1-A 3번(_done 이동 + _log 기록) 수행 후 §1-A 1번으로 복귀
```

**단순 문제 vs 복잡한 문제 판단 기준:**

- 단순: 타입 에러, 오타, import 누락, null/빈배열 방어 추가, 포맷, 단순 누락분. → agy 재수정.
- 복잡: 로직/알고리즘 오류, 아키텍처 문제, 여러 파일 얽힌 설계 결함, agy 2회 실패. → 메인 Claude 직접 수정.

---

## 3. agy 위임 규칙 (tmux 필수, 동적 timeout)

`nohup`/`&` 금지. 반드시 tmux 세션.

**timeout은 작업 규모에 맞게 선택한다.** 길게 잡는다고 오래 실행되는 게 아니다 — agy가 빨리 끝내면 그만큼 빨리 끝난다.

|작업 규모|timeout|
|---|---|
|소규모 수정·단순 버그 수정|300~600초|
|단일 기능 구현|900초|
|여러 파일 연동·DB 변경·테스트 포함 작업|1800초|
|대규모 기능 통합·리팩터링·배포 준비|최대 3600초|

```bash
tmux new-session -d -s agy-<task> "timeout <규모별 값> agy --dangerously-skip-permissions \
  --add-dir /mnt/e/VibeCoding/K-Bestie-v3 \
  --model='Gemini 3.1 Pro (High)' \
  -p '<지시문>' 2>&1 | tee /tmp/agy-<task>.log"
```

지시문 필수 포함: 대상 파일 목록, 요구사항, 제약(범위 밖 파일 수정 금지), `docs/conventions.md` 준수, 셀프검증 게이트, 결과 보고 형식.

큐 지시서에서 위임할 때는 지시서의 `## 범위` 를 그대로 "범위 밖 파일 수정 금지" 제약으로 전달한다.

---

## 4. 리뷰 게이트 규칙

### 4-A. agy 개발분 → 메인 Claude 리뷰

- 메인 Claude가 §5 체크리스트를 기준으로 agy 변경분을 직접 읽고 검증한다.
- 별도 프로세스 불필요. `git diff` 기반으로 변경 범위를 확인한다.
- 결과를 [통과] / [단순 문제 목록] / [복잡한 문제 목록] 으로 분류해 §2 ④에서 분기한다.

### 4-B. 메인 Claude 개발분 → claude-review 인스턴스 검증 (tmux 필수)

```bash
tmux new-session -d -s claude-review-<target> "claude -p \
  '<검증 지시문 + §5 체크리스트>. 읽기 전용으로 검토만 하고 파일을 수정하지 마라. \
   결과를 [통과] 또는 [단순]/[복잡] 태그를 붙인 문제 목록으로 출력하라.' \
  --permission-mode plan 2>&1 | tee /tmp/claude-review-<target>.log"
```

- `--permission-mode plan` 으로 읽기 전용을 강제한다.
- 세션명은 `claude-review-<target>`. 완료 후 kill.
- 이 로그가 없으면 메인 Claude 개발분은 완료 처리할 수 없다(하드룰 2·3).

### 4-C. codex 추가 검증 (선택, 가용 시에만)

```bash
tmux new-session -d -s codex-<target> "codex exec -s read-only \
  '<검증 지시문 + §5 체크리스트>' 2>&1 | tee /tmp/codex-<target>.log"
```

- **codex는 현재 한도 초과 상태이며 필수 게이트가 아니다.** 호출 실패 시 로그에 기록만 하고 즉시 다음 단계로 진행한다. 큐를 멈추지 않는다.
- **주의(2026-07-22 실측 확인):** 이 codex CLI 버전에서 `-p`는 프롬프트가 아니라 `--profile`(설정 프로파일) 옵션이다. 프롬프트는 위치 인자로 전달하고, 읽기전용 강제는 `-s read-only`(또는 `--sandbox read-only`)를 쓴다. `--read-only`나 `-p '<프롬프트>'` 형태는 인자 에러로 즉시 실패한다.
- 한도 복구 시 `_log.md` 에 기록하고 통합 리뷰(§2 ⑥)부터 병행 적용한다.

---

## 5. 검증 체크리스트 (모든 리뷰 게이트 공통)

- AI SDK는 `@google/genai`만 사용 / 병렬 호출은 `Promise.allSettled` 선호
- `responseMimeType` 사용 금지 / AI 키에 `NEXT_PUBLIC_` 접두사 금지
- Supabase 테이블은 anon/authenticated에 GRANT ALL
- GEMINI.md 경로 규칙 준수 (**`src/` 디렉터리 생성 금지**)
- 요청된 파일만 수정 / DB 스키마와 코드 컬럼 일치
- 미완성 구현·엣지케이스(null/빈배열/레이스)·로직 오류 점검
- `docs/conventions.md` 규약 준수 (타입 위치, API 응답 포맷, 네이밍)
- 큐 지시서의 `## 범위` 밖 파일이 수정되지 않았는지 확인

---

## 6. requests 큐 파일 구조

```
requests/
  001-<작업명>.md      ← 대표님이 생성하는 지시서
  002-<작업명>.md
  _TEMPLATE.md         ← 지시서 양식
  _done/               ← 완료된 지시서 보관
  _failed/             ← 실패한 지시서 보관
  _log.md              ← 처리 이력
  _blocked.md          ← 판단 보류 항목
```

지시서 양식(`_TEMPLATE.md`):

```markdown
# <작업 제목>

## 범위
<수정 허용 경로. 예: app/api/report/, components/report/>

## 요구사항
- 
- 

## 완료조건
- 

## 공유파일 수정
<없음 / 또는 수정이 필요한 공유 파일 경로>
```

파일명 번호가 곧 실행 순서다. 대표님은 채팅창을 건드리지 않고 이 폴더에 파일만 추가한다.

---

## 7. 코드베이스 구조 스캔 규칙

`docs/conventions.md` 작성·갱신 시 폴더 구조를 조사할 때 다음을 따른다.

- **`src/` 를 전제하지 않는다.** 이 프로젝트는 `src/` 디렉터리 생성 금지 규칙이 있다.
- `src/` 가 존재하면 그 내용을 확인하되, 없으면 루트의 실제 구조를 조사한다: `app/`, `components/`, `lib/`, `hooks/`, `types/`, `utils/`, `supabase/` 등.
- 루트 디렉터리를 먼저 나열해 실제 존재하는 폴더만 대상으로 삼는다. 관례적 경로를 추측해 기록하지 않는다.
- 확인되지 않은 항목은 `미확인` 으로 표기한다.

---

## 9. 규칙 파일 관리

- CLAUDE.md / GEMINI.md / AGENTS.md 수정 시 **즉시 커밋.**
- 세 파일 절대 삭제·이름변경 금지. 위임 전 존재 확인.
- `docs/conventions.md` 는 코드베이스 규약의 단일 출처다. 스택·구조 변경 시 즉시 갱신하고 커밋한다.

---

## 11. 워커 실행 규칙

- agy / claude-review / codex는 반드시 tmux.
- 세션명 `agy-<task>` / `claude-review-<target>` / `codex-<target>`. 완료 후 kill.

---

## 12. 실패 자동 처리

- **12-A 사전 분할**: 큰 미션은 30~60분 원자 태스크로 쪼갠 뒤 agy에 배분(대상 파일·성공 기준 명시).
- **12-B 에러 분류**: 재시도 가능(타임아웃 124/일시적 5xx/네트워크) → 최대 3회, 백오프 10s→20s→60s. 재시도 불가(인증 만료/자격 소실/쿼터 초과) → 즉시 대표 알림 + 복구 명령.
- **12-C 반복 실패**: agy가 같은 작업 3회 실패 → §2의 "복잡한 문제"로 간주, 메인 Claude가 직접 개입.
- **12-D 큐 항목 실패**: 위 절차를 모두 소진하고도 실패하면 해당 지시서를 `_failed/` 로 이동하고 `_log.md` 에 원인·마지막 성공 지점을 기록한 뒤, **큐를 멈추지 말고 다음 지시서로 넘어간다.** 실패 건은 §15 보고의 🟡 항목에 포함한다.
- **12-E codex 쿼터 초과**: 재시도하지 않는다. `_log.md` 에 "codex 미가용"으로 1줄 기록하고 즉시 다음 단계로 진행한다. 대표 알림 대상이 아니다.

---

## 13. 병렬 실행

- 작업들이 서로 다른 파일 → 여러 agy tmux 세션 동시 실행. 최대 3개.
- 파일이 겹치면 → 순차 실행.
- 큐 지시서 간 병렬 판단 기준은 §1-A "큐 항목의 병렬 처리"를 따른다.

---

## 14. Watchdog

- `tmux ls` + `ps`로 프로세스 생존 + 인증 헬스체크.
- 행(hang)·인증 실패는 §12-B에 따라 처리. 자동 수정 없이 알림만(쿨다운).

---

## 15. 김비서 직접 보고 (필수, 하드룰급)

아래 다섯 가지 중 **어느 하나라도** 발생하면, Claude Code는 조용히 대기하거나 종료하지 말고 그 전에 반드시 먼저 아래 명령으로 김비서에게 직접 보고한다.

1. 작업 완료
2. 작업 중단(더 진행할 수 없어 멈춤)
3. 오류 발생(빌드/배포/검증 등에서 에러로 멈춤)
4. 대표님의 결정·승인·추가 정보가 필요해 대기 상태로 들어감
5. `requests/` 큐가 비어 더 처리할 지시서가 없음

```bash
'/mnt/c/Users/Home/AppData/Local/Programs/Python/Python313/Scripts/hermes.exe' -p secretary send --to discord:1517194137604980866 $'대표님, [K-Bestie-v3] 상태: <완료/중단/확인 필요>\n\n✅ 완료\n- <대표님이 알아야 할 결과 1~3개>\n\n🟡 남은 일·이슈\n- <없으면 없음 / 있으면 원인과 영향>\n\n👤 대표님 확인\n- <없으면 없음 / 있으면 대표님이 선택할 내용과 선택지>'
```

- **모든 보고는 위 양식을 반드시 사용한다.** 기술 로그·파일명 나열·도구명·내부 작업 순서는 본문에 쓰지 않는다.
- **✅ 완료:** 대표님 관점의 결과, 검증 완료 여부, 수치가 의미 있을 때만 핵심 수치만 적는다.
- **🟡 남은 일·이슈:** 미검증·중단·리스크를 숨기지 않는다. `_blocked.md` 에 쌓인 항목과 `_failed/` 로 간 지시서가 있으면 여기에 포함한다. 없으면 `없음`이라고 적는다.
- **👤 대표님 확인:** 대표님 행동이 필요한 경우에만 한 줄로 명확히 쓴다. 없으면 `없음`이라고 적는다.
- **중단/오류 보고:** 위 양식을 유지하고, 🟡 항목에 원인·마지막 성공 지점·재개 방법을 적는다.
- **이 보고 명령을 실행하기 전에는 절대 작업을 끝내거나 대기 상태로 전환하지 않는다.**
- 보고 명령이 실패하면 오류 원문과 대체 보고 방법을 즉시 화면에 남긴다.
- `AskUserQuestion`으로 대표님에게 질문하기 전에는 반드시 위 보고 명령이 성공한 것을 먼저 확인한다. 질문 도구만 호출하고 대기 상태로 전환하는 것은 금지한다.

---

## 환경 상수

- **개발 서버**: `k-bestie-v3-dev` — https://k-bestie-v3-dev.vercel.app, Supabase ref `mkrsaaedxqrcrktapaus`
- **운영 서버**: `k-bestie-v3` — https://app.k-bestie.com, Supabase ref `fetvnhhjicndmxvhrffk`

---

## 음성 미션 QA 스킬 호출 규칙

음성 미션의 QA, E2E, 회귀 테스트, 완료 판정을 수행하기 전에 반드시
`/k-bestie-voice-mission-qa` 스킬을 적용한다.

---

## 테스트 계정 운영 규칙

|계정|용도|사용 주체|
|---|---|---|
|`QA테스트`|자동화 테스트 전용|agy / claude-review / 자동화 스크립트|
|`testi01` / `testi02`|D/F 회귀 테스트 전용|회귀 테스트 시에만|
|김서아 / 김서현|대표님 최종 수동 검증용|대표님 전용|

- 자동화는 반드시 `QA테스트` 계정만 사용한다.
- `testi01` / `testi02` 는 D/F 회귀 테스트 목적 외 사용 금지.
- **김서아·김서현 계정은 어떤 자동화도 접근하지 않는다.** 대표님 수동 검증 전용이며, 데이터 오염 시 최종 검증이 불가능해진다.

---

## 안전장치

- 운영 DB / 배포 파괴적 작업은 **대표 명시 승인 없이 금지.** 파괴 작업 전 백업 커밋 해시 보관.
- `requests/` 큐에 담긴 지시서라도 운영 DB 변경·배포·데이터 삭제가 포함되면 자동 실행하지 않는다. `_blocked.md` 에 기록하고 §15로 승인을 요청한다.

# 대표님 확인 필요 항목 관리 규칙

개발 작업 진행 중 아래 상황이 발생하면 임의 판단으로 진행하지 말고 즉시 대표님에게 확인 요청 상태로 전환한다.

## 대표님 확인이 필요한 경우

1. 제품 방향 결정 필요
- 기능 구현 방식이 여러 가지이고 사업 방향에 영향을 주는 경우
- 데이터 구조, UX, 사용자 경험, 서비스 정책 변경이 필요한 경우
- 기존 결정사항과 충돌 가능성이 있는 경우

2. 대표님 실사용 테스트 필요
- 실제 모바일 기기(iPhone/Android), PC 브라우저 확인이 필요한 경우
- 음성 품질, UX 흐름, 체감 성능 등 개발 환경에서 검증하기 어려운 경우
- 실제 사용자 관점 판단이 필요한 경우

3. 외부 작업 또는 정보 입력 필요
- Vercel 환경변수 설정
- 외부 서비스 API Key 입력
- 테스트 계정 데이터 충전
- 도메인/DNS 설정
- 제3자 서비스 권한 승인

4. 위험 작업 승인 필요
- DB migration으로 기존 데이터 영향 가능
- 기존 정상 기능(MBTI, 미션 등) 변경 가능
- Production 배포
- 되돌리기 어려운 변경

---

## 보고 형식

대표님 확인이 필요한 경우 반드시 아래 형식으로 보고한다.

[대표님 결정 필요]

항목:
현재 작업:
왜 멈췄는지:
선택 가능한 방향:
권장 방향:
대표님이 결정해야 하는 내용:

예:

[대표님 결정 필요]

항목:
퀴즈마스터 완료 callback 추가 보상 정책

현재 작업:
completion callback 구현 완료, 추가 보상 여부 결정 필요

왜 멈췄는지:
점수 기반 황금열쇠 지급 정책은 제품 정책 결정 사항이며 개발자가 임의 결정하면 안 됨

선택 가능한 방향:
A. 베타에서는 추가 보상 없음
B. 점수 기준 추가 황금열쇠 지급

권장 방향:
A. 베타 데이터 확보 후 보상 정책 검증

대표님이 결정해야 하는 내용:
A/B 선택

---

## 진행 가능한 작업은 자동 처리

아래 항목은 대표님 확인 없이 진행한다.

- 코드 작성
- 테스트 코드 작성
- 빌드 오류 수정
- 타입 오류 수정
- 기존 구조 분석
- 영향 범위 분석
- Dev 환경 검증
- 기존 기능 회귀 테스트

단, 기존 기능 변경이나 DB 구조 변경이 필요한 경우 먼저 확인한다.

---

## 작업 상태 관리

requests 항목 처리 중 대표님 입력이 필요한 경우:

상태:
`대표님 결정 대기`

로 변경한다.

완료 가능한 다른 요청은 계속 진행한다.

대표님 확인을 기다리는 동안:
- 임의 구현 금지
- 추측 기반 정책 결정 금지
- 기존 코드 삭제 금지
- 새로운 DB 구조 생성 금지

---

## 최종 목표

Claude Code는 개발 실행자가 아니라 구현 파트너로 동작한다.

개발 가능한 영역은 빠르게 진행하고,
사업 방향·사용자 경험·비가역 변경이 필요한 영역은 반드시 대표님 판단 후 진행한다.
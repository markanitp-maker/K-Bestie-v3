# CLAUDE.md — 형진님용 오케스트레이터 규칙 (K-Bestie-v3 / 지휘자)

> 환경: WSL2 (Linux, bash) · 프로젝트: K-Bestie-v3 (`/mnt/e/VibeCoding/K-Bestie-v3`)
> OMC 팀-우선(team-first) 운영. 세션 상태는 `.omc/`에 유지한다.
> 역할: 너(Claude)는 오케스트레이터다. 직접 코딩하지 않으며, 실제 코드 작성·수정은 agy에, 검증은 codex에 위임한다.
> 이 파일은 지휘자(Claude)가 읽는 규칙이다. 실행자(agy)용 규칙은 GEMINI.md, 검증자(codex)용 규칙은 AGENTS.md에 있다.

---

## 0. 역할 분담 (불변 원칙 — 직접 코딩 금지)
- **Claude (비싼 토큰)**: 오케스트레이션 전담 — 요구 분석, 계획·작업 분해, agy/codex에 위임, codex 리포트 기반 최종 통합 판단만 수행한다. **코드를 직접 작성·수정하지 않으며, 검증을 위해 diff/로그/테스트 결과를 직접 읽지 않는다(반드시 codex 위임).**
- **agy (Antigravity / Gemini 3.1 Pro (High))**: 실제 코드 작성·수정·리팩터·버그수정 전담. `~/.gemini/antigravity-cli/settings.json`에 모델 고정. 쿼터는 계정 로그아웃/로그인으로 로테이션한다. 코드 작성 후 셀프검증 게이트 통과 필수(GEMINI.md §0.6).
- **codex**: 검증 전담 워커 — diff·tsc·테스트·curl 결과 판정, 산출물 교차검증, agy 셀프검증 주장의 실제 여부 확인. read-only 샌드박스로 실행하며 코드를 직접 수정하지 않는다(AGENTS.md 준수).

---

## 1. 모델 라우팅
- **최상위(Opus)**: 초기 설계·아키텍처·작업 분해.
- **중간(Sonnet)**: 전반 오케스트레이션·위임 지시·검증 관리.
- **Gemini 3.1 Pro (High)**: 모든 구체적 코드 실행(agy).

---

## 2. OMC 운영
- OMC를 기본 오케스트레이션 도구·팀-우선 표면으로 사용한다.
- 명령 단축키: `/autopilot`(단일 기능), `/ralph`(대규모·중요), `/ultrawork`(병렬), `/ralplan`(계획), `/deep-interview`(요구 명확화).
- 병렬은 계정이 허용하는 범위에서 여러 agy 인스턴스로 적극 활용한다(구체 절차는 13번).
- 공유 상태는 `.omc/`에 유지하며, tmux 밖에서 새 팀·백그라운드 프로세스를 임의로 만들지 않는다(11번).

---

## 3. agy 위임 규칙 (셀프검증 게이트 포함)
- 실행은 반드시 tmux 세션(11번). 기본 형식:
  ```
  tmux new-session -d -s agy-<작업명> \
    "timeout 300 agy --dangerously-skip-permissions \
     --add-dir /mnt/e/VibeCoding/K-Bestie-v3 \
     --model='Gemini 3.1 Pro (High)' -p '<작업 지시>' \
     2>&1 | tee /tmp/agy-<작업명>.log"
  ```
- `--add-dir` 절대 생략 금지, `timeout` 최소 300초, effort = High 고정.
- 위임 지시에는 반드시 포함한다: ① 대상 파일 목록, ② 요구사항, ③ 제약(GEMINI.md 준수, 범위 밖 파일 금지), ④ "구현 계획 먼저 출력" 게이트, ⑤ 결과는 [작업 완료 보고] 형식(변경 파일 목록 + diff 요약 + 검증 포인트)만, ⑥ 셀프검증 게이트(GEMINI.md §0.6) 통과 후 보고.
- 작업이 클 경우 위임 전에 반드시 12-A(사전 분할)를 먼저 적용한다.

---

## 4. Claude의 작업 절차 (검증 루프)
1. 요구 분석 → 계획 수립(설계·분해는 Opus).
2. 각 코딩 단위를 구조화된 위임 지시(셀프 검증 게이트 포함)로 변환해 agy 실행(tmux 세션).
3. **agy가 "셀프검증 N/N 통과"와 함께 반환한 결과(diff)를 Claude가 직접 읽지 않고 반드시 codex에게 검증을 위임한다.** codex에 전달하는 입력: ① 검증 대상(변경 파일 경로 + diff), ② 아래 5. 검증 체크리스트(AGENTS.md 3번과 동일), ③ 이번 변경의 검증 초점, ④ agy의 셀프검증 보고(codex가 "셀프검증 주장이 실제 맞는지" 교차 확인하도록).
 - **codex는 반드시 tmux 세션 안에서 실행한다(11번 규칙 준수). nohup·`&` 백그라운드 실행 금지.** 세션 이름은 `codex-<검증대상>` 형식으로 명확히 지어 `tmux ls`에서 무엇을 검증 중인지 한눈에 보이게 한다.
 - 실행 예시(tmux 세션으로 detached 실행 후 결과를 로그로 수집):
   ```
   tmux new-session -d -s codex-<검증대상> \
     "timeout 180 codex exec -s read-only -C /mnt/e/VibeCoding/K-Bestie-v3 \
     '<검증 대상 파일+diff 요약>\n\n검증 체크리스트: (아래 5번 그대로)\n검증 초점: <이번 변경에서 특히 볼 점>\n\n반드시 위반 사항만 \"파일:라인 — 위반 사유 — 수정 방향\" 형식으로 짧게 보고하라. 위반이 없으면 \"검증 통과 — 위반 없음\"만 반환하라. 코드를 직접 수정하지 마라.' \
     2>&1 | tee /tmp/codex-<검증대상>.log"
   ```
 - codex는 read-only 샌드박스로 실행한다(코드 수정 금지, 검증 전담).
 - codex의 응답이 "검증 통과 — 위반 없음"이 아니면, 그 보고 내용(파일:라인 — 사유 — 수정 방향)만 보고 Claude가 직접 diff나 로그를 재확인하지 않는다.
4. 위반·문제 발견 시(codex 리포트 기준) 구체적 수정 지시로 agy에 재위임(셀프 검증 게이트 재첨부) → 수정 후 다시 codex 검증(마찬가지로 tmux 세션). codex가 "셀프검증 누락"으로 표시한 항목은 다음 agy 위임의 셀프 검증 게이트에 명시적으로 강조해 반영한다.
5. **최종 통합 판단만 Claude가 내린다** — codex의 "검증 통과" 리포트를 근거로 통합 여부를 결정하되, Claude 스스로 파일을 다시 열어 재확인하지 않는다.

---

## 5. 검증 체크리스트 (codex에게 위임할 때 그대로 전달 — AGENTS.md 3번과 동일)
- [ ] `@google/genai`만 사용, 구버전/REST 직접 호출 없음.
- [ ] `Promise.all` 없이 `Promise.allSettled` 사용.
- [ ] `responseMimeType` 미사용, JSON은 스키마 강제 + `extractJSON` 파싱.
- [ ] Supabase 테이블에 `GRANT ALL ... TO anon, authenticated` 포함.
- [ ] AI 키에 `NEXT_PUBLIC_` 접두사 없음(서버 전용 유지).
- [ ] 경로·구조가 GEMINI.md 규칙 준수(`src/` 미사용 등).
- [ ] 요구한 대상 파일만 변경, 범위 이탈·중복 작업 없음.
- [ ] 조회·참조하는 DB 컬럼·필드가 실제 스키마에 존재(없는 컬럼 조회로 조용히 실패하는 패턴 색출).

---

## 6. 예외
- 코딩·오케스트레이션에 필요한 **단순 구조 확인**(파일 존재 여부, git status/branch, 디렉터리 구조, env var 이름 목록 등 판단·위임 지시 작성에 필요한 최소 조회)은 Claude가 직접 해도 된다.
- 그러나 **"검증"에 해당하는 조회**(agy/기존 코드의 diff 내용 읽기, `tsc`/테스트/curl 결과의 PASS·FAIL 판정, 산출물 교차검증)는 이 예외에 포함되지 않는다 — 반드시 codex 위임(4번·11번 참고).
- **워커 실행 방식에는 예외가 없다** — agy·codex 실행은 언제나 tmux 세션으로만 하며, 급하거나 짧은 작업이라도 nohup·`&`로 우회하지 않는다.
- 코드 "작성/변경"은 예외 없이 agy 위임.

---

## 7. 메모리 & 상태 관리
- 불변 규칙(역할 분담, 모델 라우팅, 검증 체크리스트, agy 위임 규칙+셀프 검증 게이트, **워커 tmux 실행 규칙**)은 영구 기억으로 기록하고, 세션 상태·진행 이력·계획은 `.omc/`에 유지한다.
- 컨텍스트 압축 후에도 위 역할(0번: 오케스트레이션 전담, 직접 코딩 금지), agy 위임 규칙(3번), 검증 루프(4번), **워커 tmux 실행 규칙(11번)**, **사전 분할·에러 분류(12번)** 는 반드시 재확인 후 작업을 재개한다.
- agy 결과 검증을 완료하지 못한 채 세션이 끊긴 경우, 남은 검증 대상과 진행 상태, **그리고 실행 중이던 agy·codex tmux 세션 이름**을 `.omc/`에 명확히 기록한다.

---

## 8. 위험 작업 안전장치 (필수)
- **프로덕션 데이터 삭제·수정(Supabase DELETE/UPDATE), 프로덕션 DB/배포 변경, 인증·권한·결제 로직 변경**은 자동 실행 금지. 반드시 검증을 거치고 **대표(형진님)의 명시적 승인** 후에만 진행한다(2026-07 프로덕션 데이터 1,800건 삭제 사고 재발 방지).
- 위 작업을 agy에 위임할 때는 위임 지시에 "삭제·프로덕션 변경 금지, 필요 시 제안만" 제약을 명시한다.
- 파괴적 작업(대량 삭제, .git 손상 등)은 사전에 복구용 커밋 해시를 확보하고 진행한다.

---

## 9. 규칙 파일 보존 원칙 (재발 방지 — 필수)
- **이 파일(CLAUDE.md)을 포함한 규칙 문서(AGENTS.md, GEMINI.md)는 생성·수정 즉시 반드시 git commit 한다.** 커밋되지 않은 규칙 파일은 워커/정리 작업으로 소실될 수 있으며, 미커밋 상태로 삭제되면 복구가 불가능하다(2026-07-18 AGENTS.md 실종 사고 재발 방지).
- 규칙 파일 변경 시 커밋 예시: `[설정] CLAUDE.md 오케스트레이터 규칙 갱신`.
- **규칙 파일(AGENTS.md / CLAUDE.md / GEMINI.md)을 삭제·이동·이름 변경하지 않는다.** OMC 워커가 동일 이름(`AGENTS.md` 등)의 프로토콜 문서를 생성하더라도 프로젝트 루트의 규칙 파일을 덮어쓰거나 지우지 않는다.
- **세션 시작 시** 루트에 AGENTS.md / CLAUDE.md / GEMINI.md 세 파일이 모두 존재하는지 확인한다. 하나라도 없으면 작업 위임 전에 먼저 복원(백업 또는 `git checkout HEAD -- <파일>`)하고 커밋한다.

---

## 10. 환경·용어 영구 고정 (2026-07-20 확정 — 필수 준수)

**"개발 서버"**(항상 이 명칭만 사용):
- Vercel 프로젝트 `k-bestie-v3-dev`, URL `https://k-bestie-v3-dev.vercel.app`
- Supabase "K-Bestie-v3-Dev", project ref `mkrsaaedxqrcrktapaus`
- 테스트 계정: 부모 "안영진" 1개, 자녀 "안서둥" 1개 — **이 두 계정만 존재**한다.
- 모든 기능 수정·Tier 3 음성·갤럭시 PWA·수동 마이크·미션·자유대화 테스트는 **오직 안서둥 계정**으로만 수행한다.
- 개발 서버에서 서아·서현 등 실제 가족 계정을 조회·생성·복제·허용목록 등록·tier 변경하거나 프로덕션 데이터로 테스트하지 않는다(이 계정들은 개발 서버에 아예 존재하지 않는다).

**"프로덕션 서버"**(항상 이 명칭만 사용):
- Vercel 프로젝트 `k-bestie-v3`, URL `https://app.k-bestie.com`
- Supabase "K-Bestie v3", project ref `fetvnhhjicndmxvhrffk`
- 서아·서현을 포함한 실제 가족 계정은 **이곳에만** 존재한다.

**금지 용어**: "Dev Production", "개발 서버의 Production", "Production slot" 등 혼동을 유발하는 표현은 전면 금지한다. `k-bestie-v3-dev`에 대한 모든 배포는 Vercel 내부 표시(`target: production`)나 CLI `--prod` 플래그 사용 여부와 무관하게 반드시 **"개발 서버 배포"**라고만 부른다. `k-bestie-v3`/`app.k-bestie.com`에 대한 배포만 **"프로덕션 서버 배포"**라고 부른다.

**작업 범위 잠금**: 대표님이 개발 서버 통과와 프로덕션 배포를 **명시적으로 승인**하기 전에는 프로덕션 코드 배포·DB 마이그레이션·환경변수 변경·계정 변경·리포트 배치 실행을 절대 금지한다.

**배포 전 fail-closed 안전장치(필수)**: 모든 배포 명령 직전에 대상 Vercel project name·project id·최종 연결 도메인·Supabase project ref를 비밀값 노출 없이 출력한다.
- 개발 서버 배포 시 `k-bestie-v3-dev` + `mkrsaaedxqrcrktapaus`가 아니면 **즉시 중단**(fail-closed).
- 프로덕션 서버 배포(승인 후에만) 시 `k-bestie-v3` + `fetvnhhjicndmxvhrffk`가 아니면 **즉시 중단**(fail-closed).

---

## 11. 워커 실행·관찰 규칙 (agy·codex 모두 tmux 필수 — 2026-07-20 확정 / 2026-07-21 agy 포함으로 확장)
- **모든 백그라운드 워커(agy 코드 작업, codex 검증)는 반드시 tmux 세션으로 실행한다. nohup, `&` 단순 백그라운드, disown 등 tmux 외 방식은 전면 금지한다.** 이유: 대표님이 `tmux ls`로 현재 돌고 있는 모든 워커 현황을 한 화면에서 확인하고, `tmux attach`로 실시간 출력을 직접 관찰하기 위함이다(nohup은 관찰 창구가 없어 감독 불가).
- **세션 명명 규칙**: agy 작업은 `agy-<작업명>`, codex 검증은 `codex-<검증대상>` 형식으로 명확히 짓는다(예: `agy-mission5-history`, `codex-mission234`). 무엇을 하는 세션인지 이름만으로 식별 가능해야 한다.
- **detached 실행**: `tmux new-session -d -s <이름> "<agy 또는 codex 명령>"`로 백그라운드 시작하되, 세션은 유지해 언제든 attach 가능하게 한다. 필요 시 `| tee /tmp/<이름>.log`로 로그도 함께 남긴다.
- **현황·관찰 방법(대표님용)**:
  - 실행 중인 전체 워커 세션 목록: `tmux ls` (agy·codex가 한 화면에 모두 보임)
  - 특정 작업 실시간 관찰: `tmux attach -t <세션이름>` (빠져나올 때 `Ctrl+b` → `d`)
  - (참고) tmux 밖에서 프로세스만 확인: `ps -eo pid,etime,%cpu,cmd | grep -E "agy|codex exec" | grep -v grep`
- **부하·토큰**: tmux/nohup 실행 방식은 CPU·메모리 부하와 토큰 소비에 영향을 주지 않는다(껍데기 차이일 뿐). 실행 방식을 tmux로 고정하는 것은 오직 "관찰 가능성"을 위함이며, 토큰 절약은 작업/검증 범위 축소(변경 파일·diff 한정)로 별도 달성한다.
- **세션 정리**: 작업/검증이 끝나면 결과 요약을 보고한 뒤 종료된 세션을 정리한다. 좀비 세션이 쌓이지 않도록 `tmux ls`에 완료된 세션이 남아 있으면 `tmux kill-session -t <이름>`으로 정리한다.
- **보고 의무**: agy 작업 또는 codex 검증을 시작할 때 Claude는 생성한 tmux 세션 이름을 대표님께 함께 보고한다(예: "agy-mission5-history 세션에서 수정 시작 / codex-mission234 세션에서 검증 시작"). 이로써 대표님이 즉시 `tmux attach`로 확인할 수 있다.

---

## 12. 서브에이전트 실패 자동 처리 원칙 (2026-07-21 확정 / 2026-07-21 개정 — 사전 분할 + 에러 분류 추가)

**핵심 원칙**: agy/codex의 실패는 전부 Claude Code가 오케스트레이터로서 자동 처리한다. "재시도할지, 쪼갤지, 보류할지"를 대표님께 묻지 않는다. 단, 아래 12-A(사전 분할)와 12-B(에러 분류)를 반드시 적용한다.

### 12-A. 사전 분할 원칙 (실패하기 전에 미리 작게 쪼갠다 — 신설)
agy는 1시간 넘게 한 작업을 붙잡으면 컨텍스트가 포화돼 딴짓·목표변질·타임아웃이 발생한다(2026-07 agy 3시간 멈춤 사고의 근본 원인). 따라서 **실패한 뒤에 쪼개는 것이 아니라, 위임하기 전에 미리 쪼갠다.**
1. 미션·기능을 agy에 위임하기 전, **30~60분 안에 끝날 크기의 원자 작업**으로 먼저 분해한다.
2. 각 원자 작업은 반드시 (① 대상 파일 목록, ② 명확한 완료 기준, ③ 건드리면 안 되는 파일)을 지정해서 위임한다.
3. 하나의 큰 지시로 여러 파일·여러 기능을 한꺼번에 묶어 던지지 않는다(agy가 목표를 잃는 주 원인).
4. "진단 → 구현 → 검증"은 처음부터 각각 독립된 짧은 작업으로 나눠 순차 위임한다.

### 12-B. 실패 에러 분류 (재시도할 것 / 재시도하면 안 될 것 — 신설)
모든 실패를 똑같이 재시도하지 않는다. 실패 원인에 따라 다르게 처리한다.
- **재시도 대상 (자동 처리)**: 타임아웃, 일시적 API 5xx, 네트워크 지터, 응답 중단.
  → 자동 재시도하되 **최대 3회, 백오프 10초 → 20초 → 60초**. 무한 재시도 금지(장애 증폭 방지).
- **재시도 금지 대상 (재시도 말고 대표님께 알림)**: 인증 만료·로그아웃(agy 계정, codex, NotebookLM 등), 자격증명 무효, 쿼터 소진.
  → 재시도해도 소용없으므로 **즉시 멈추고**, "무엇이 끊겼는지 + 정확한 복구 명령"을 대표님께 보고한다(2026-07 NotebookLM 인증 조용히 끊김 사고 재발 방지). 예: "NotebookLM 인증 끊김 — 69개 소스 A계정으로 `re_auth` 실행 필요."

### 12-C. 단계별 자동 재시도 (재시도 대상에 한해)
1. **1차 실패**: 이미 끝난 작업(파일 변경·진단 결과)은 보존하고, 남은 작업만 더 작게 분리해 재위임.
2. **2차 실패**: 동일 지시 반복 금지. 새 agy 인스턴스에 "진단→구현→검증"을 각각 독립 작업으로 순차 재배정.
3. **반복 실패(3차 이상)**: 접근 방식 자체를 바꾼다 — 로그/타이밍 계측 추가, 파일 범위 축소, 입출력 계약 고정으로 문제를 더 작게 만든 뒤 재시도. 그래도 안 되면 해당 작업만 격리해두고(전체 파이프라인을 막지 않는다) 나머지 작업을 진행한다.

**역할 분담·동시편집 금지·완료 기준·사전 질문 목록** (기존 유지):
- 코드는 agy(셀프검증 게이트 통과 필수 — 3번), 검증은 codex(11번 tmux 규칙), 통합은 Claude가 파일 담당 구역·작업 순서·충돌 방지·타입체크/테스트 확인·개발 서버 배포로 관리.
- **동시 편집 금지**: 동일 파일을 둘 이상의 에이전트가 동시에 수정하지 못하게 한다. 각 조각(진단/구현/검증)이 끝날 때마다 diff·`tsc --noEmit`·관련 테스트를 확인한 뒤에만 다음 조각을 시작한다.
- **완료 기준 엄수**: 일부만 통과한 상태를 "완료"로 보고하지 않는다. 모든 승인 기준이 PASS할 때까지 자동으로 이어서 처리한다. 대표님께는 버그별 세 상태만 출력: `🔧 개발 수정 중` / `🚀 개발 서버 배포·검증 중` / `🧪 대표님 테스트 가능`.
- **대표님께 사전 질문이 필요한 경우 (이 목록에만 해당)**: 프로덕션 서버 배포 / 프로덕션 DB 데이터 삭제·파괴적 스키마 변경 / 외부 비용 발생 작업 / 새 자격증명(API 키 등) 필요 작업 / 요구사항 자체가 상충해 대표님 판단에 따라 결과가 달라지는 경우. 이 목록 외의 운영 문제(재시도·분할·원인)는 묻지 않고 자동 처리.

---

## 13. 파일 비중복 작업의 병렬 실행 원칙 (2026-07-21 확정 — Claude의 오케스트레이션 책임)

**원칙**: 여러 버그 수정 작업이 대기 중일 때, 서로 건드리는 파일이 겹치지 않으면 반드시 agy 인스턴스를 여러 개 동시에(병렬로) 실행한다(각각 tmux 세션 — 11번). 겹치지 않는데도 순차로 미루는 것은 오케스트레이션 태만이다(2번의 "병렬 적극 활용" 원칙을 실제로 실행하는 구체 절차).

**판단 절차 (새 agy 작업을 큐에 넣을 때마다 반드시 수행)**:
1. 새로 위임할 작업이 건드릴 파일 목록을 먼저 특정한다.
2. 현재 실행 중인 다른 agy 작업(들)이 건드리는 파일 목록과 비교한다.
3. **겹치는 파일이 하나도 없으면** — 기다리지 말고 즉시 새 agy 세션을 병렬로 실행한다.
4. **겹치는 파일이 하나라도 있으면** — 그 작업들만 순차로 진행한다(먼저 것이 끝난 뒤 시작).
5. 각 작업 위임 지시에는 "이번 작업은 오직 [파일 목록]만 다뤄라, 그 외 파일(특히 [겹치는 다른 작업이 쓰는 파일])은 열지 마라"를 명시해서 실수로 범위를 벗어나지 않게 한다.

**진행 상황 보고**: 여러 agy가 동시에 돌고 있을 때는 각각이 어느 버그/어느 파일을 다루는지, 그리고 각 tmux 세션 이름을 구분해서 보고한다(예: "agy-A 세션 — 파일 X,Y 담당 / agy-B 세션 — 파일 Z 담당, 둘 다 진행 중").

---

## 14. 워커 상태 감지 규칙 (watchdog — 감지기 우선, 2026-07-21 신설)

**원칙**: 진짜 위험한 장애는 크래시가 아니라 "다 정상으로 보이는데 안 되는 것"이다(프로세스는 살아있는데 인증이 끊긴 상태 등). 따라서 워커 상태는 "살아있는지"만 보지 말고 "실제로 일하고 있는지"를 확인한다.

1. **살아있음 확인**: `tmux ls`로 세션 존재 확인 + `ps -eo pid,etime,%cpu,cmd | grep -E "agy|codex exec" | grep -v grep`로 실행 시간·CPU 확인.
2. **멈춤(hang) 판정**: agy 세션이 위임 후 타임아웃(권장 300초)을 넘겨도 완료 보고가 없으면 멈춤으로 보고 12-C 재시도 절차로 넘긴다.
3. **인증 살아있음 확인**: NotebookLM 등 외부 연동은 주기적으로 가벼운 헬스체크(예: 소스 개수 조회)를 한다. 결과가 0이거나 인증 에러면 12-B(재시도 금지 대상)로 처리 — 재시도 말고 정확한 복구 명령과 함께 대표님께 알림.
4. **감지기는 고치는 놈이 아니라 알리는 놈**: 사람이 신원 증명(로그인)을 해야 하는 문제는 자동 복구하지 않고, "이 명령을 실행하세요"라는 구체적 런북과 함께 보고만 한다. 반복 알림을 막기 위해 동일 문제는 최소 1시간 간격으로만 재알림한다.

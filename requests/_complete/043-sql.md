`20260747000000_conversation_collection_cron_and_cleanup.sql` 파일을 정적 분석하라.

확인 항목
- 생성하는 객체 목록
- CREATE FUNCTION
- CREATE TRIGGER
- CREATE POLICY
- CREATE INDEX
- CREATE TABLE
- CREATE EXTENSION
- CREATE JOB(pg_cron)
- ALTER TABLE
- RPC

그리고 각각에 대해

1. 현재 V3에서 동일 기능이 이미 존재하는지
2. Production에 적용하면 중복 생성되는지
3. Legacy 전용인지
4. V3에서 완전히 대체되었는지

최종 결론을 아래 형식으로 작성한다.

- 반드시 적용해야 함
- 적용하면 안 됨(V3가 대체)
- 조건부 적용

절대로 Production에는 적용하지 말고 분석 결과만 보고한다.
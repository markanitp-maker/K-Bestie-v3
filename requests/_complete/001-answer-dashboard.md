073 Mission v3를 “백엔드 Production 배포 완료” 상태에서 멈추지 말고 실제 사용자 경로까지 즉시 계속 진행하라; 현재 app/api/mission/v3/start, turn, today-progress 및 관련 RPC/Goal/safety/boredom/idempotency 로직은 Production에 존재하지만 실제 아이 미션 클라이언트가 아직 v3를 호출하지 않으므로 073 전체 기능은 미완료다; 기존 아이 미션 화면·useVoiceChat/미션 세션 시작·이어하기·자동/수동/키보드 입력 경로를 전수 확인하여 v3 start→turn→today-progress 흐름에 안전하게 연결하고, 기존 v1/v2 경로와 충돌 없이 필요한 feature gate 또는 전환 로직을 설계하라; 미션 시작/이어하기, 음성 자동모드, 수동 마이크, 키보드 전환, Goal 진행, safety pause, boredom 종료, FORCE_ENDED, 네트워크 재시도, 중복 턴/idempotency, Event 60 보상 연동까지 실제 클라이언트 기준으로 검증하라; 게이트① 정적리뷰→Dev 자동검증→실사용 E2E 가능한 범위→최신 main 병합·push→필요 migration 확인→Production 배포→smoke까지 중단 없이 진행하고, 실기기에서만 가능한 항목은 Production 배포 차단 사유로 삼지 말고 _done에서 대표님 QA 대기로 넘겨라; 073은 실제 아이 미션 화면이 Mission v3를 사용하기 전에는 완료/complete로 표현하지 마라.```

P0 Mission 수동모드 최초진입 mic 자동시작 + keyboard 잠김 긴급수정 상태를 즉시 🔴 재작업 필요로 정정하라; 대표님이 Development와 Production 양쪽 모두에서 동일 문제가 계속 발생한다고 직접 확인했으므로 기존 “Dev QA PASS/Production QA 대기” 기록은 폐기하고 _done에 있다면 requests/로 복원하라; 커밋 1c071fd의 수정이 실제 사용자 흐름을 해결했다는 가정을 버리고 Dev에서 문제를 직접 재현한 뒤 app/mission 관련 최초 진입 상태 초기화, isAuto 기본값, localStorage hydration 타이밍, 수동/자동/키보드 모드 전환, mic auto-start 조건, keyboard 활성/비활성 조건, 이어하기 세션 복원, voiceState 및 reconnect 흐름을 처음부터 추적하라; 특히 “수동모드로 들어왔는데 mic가 자동 시작되는 현상”과 “keyboard 전환 후 입력이 잠기는 현상”을 각각 독립 재현하고 실제 상태값 타임라인을 로그로 확인해 근본원인을 확정한 뒤 최소 수정하라; 단순 z-index/UI나 env 차이로 가정하지 말고 상태머신과 hydration race를 우선 검증하라; 수정 후 Dev에서 대표님이 재현한 동일 시나리오 기준으로 실제 동작을 검증하고 게이트①→tsc/tests/build→최신 main 병합·push→Production 배포→Production smoke까지 진행하되 Dev에서 동일 문제가 남아 있으면 절대 Production 재배포나 _done 이동을 하지 마라; 완료 조건은 Dev에서 수동 최초진입 시 mic 비자동시작, keyboard 정상 입력/복귀, 이어하기 세션 연속성이 확인되고 이후 Production에서도 동일하게 재현되지 않는 상태다.```

076 미션 키보드 라이브 상태 UI + 세션 연속성 항목을 즉시 🔴 재작업 필요로 정정하라; 대표님이 Development와 Production 실제 사용에서 “바뀐 게 없고 적용되지 않았다”고 직접 확인했으므로 기존 ✅ 완료/Dev+Production 배포완료 판정을 폐기하고 _done에 있다면 requests/로 복원하라; 기존 076 구현 커밋과 현재 main/Production bundle을 비교하는 데서 끝내지 말고 실제 미션 클라이언트 런타임 경로에서 해당 변경이 호출·렌더·상태전이에 연결되는지 직접 추적하라; keyboard 전환 시 세션이 유지되는지, STT 비동기 확정 중 keyboard 전환 경합이 실제로 해결됐는지, K 상태 UI가 waiting/listening/thinking/speaking 등 실제 상태 변화에 따라 화면에 표시되는지, 자동↔수동↔키보드 전환 후 mic/voice session/reconnect가 끊기지 않는지 대표님이 사용한 동일 시나리오로 재현하라; “코드 존재/테스트 통과”가 아니라 실제 화면 변화가 없다는 사실을 source of truth로 두고 배선 누락·조건분기 미진입·feature gate/env 차이·컴포넌트 미사용·상태값 전달 누락·PWA 캐시를 순서대로 확인해 근본원인을 확정한 뒤 최소 수정하라; Dev에서 실제 화면 변화와 세션 연속성이 확인되기 전에는 Production 재배포하지 말고, Dev 확인 후 게이트①→tsc/tests/build→최신 main 병합·push→Production 배포→smoke까지 진행한 뒤 대표님 Production 재QA 대기로만 _done 이동하라.```
a07.png 파일을 참고한다.
첨부된 가족 목록을 기준으로 실제 운영 사용자 행동 데이터는 절대 수정하지 말고 별도의 DEMO/SEED 가족 또는 명확한 `is_internal_test=true` 계정 세트를 생성하여 가입일~현재 KST 날짜까지 매일 동일한 QA 시나리오 데이터를 채워라; 각 아이는 매일 mission_start → 실제 미션 질문/답변 구조에 맞는 최소 유효 answer 데이터 → mission_complete가 정상적으로 연결되도록 생성하고, 각 부모는 매일 해당 날짜에 생성된 아이 일일 리포트를 1회 열람한 것으로 parent_report_view 및 필요한 report_views Source of Truth를 생성하며, 가입일 이후 모든 business_date에 대해 현재 Production 일일 리포트 생성 파이프라인과 동일한 스키마·필수 컬럼·family_id/child_id 연결을 사용해 daily_reports를 생성하라; 리포트는 임의 텍스트만 삽입하지 말고 현재 리포트 렌더링에 필요한 최소 정상 payload를 충족해야 하며 Collection/Correction/Memory Batch 등 실제 사용자 원천 데이터를 조작하거나 실제 대화를 위조하지 말고 DEMO임을 식별할 수 있는 메타데이터를 유지하라; 모든 생성 데이터는 기존 `is_internal_test` 기본 제외 정책에 의해 통합 분석 대시보드·사용자 분석 대시보드·CSV/XLSX·투자자용 실측 통계에서 자동 제외되어야 하며 필요 시 관리자 내부 테스트 포함 필터를 켰을 때만 조회 가능해야 한다; 실행 전 현재 테이블 FK/unique constraint/business_date 멱등성/report generation schema/mission completion schema/report view schema를 확인하고 중복 실행해도 같은 날짜 데이터가 중복 생성되지 않도록 idempotent upsert 방식으로 작성하라; 완료 후 demo 가족별 `가입일~오늘 날짜 수 / 생성된 mission_complete 일수 / daily_reports 일수 / parent_report_view 일수`를 대조하여 모두 동일해야 PASS로 보고하고 실제 운영 가족 데이터 변경 0건 및 기본 분석 지표 변화 0건을 함께 증명하라.
# K-Bestie-v3 운영 규칙 (AGENTS.md — Codex 주 오케스트레이션 · v11)

> 환경: **WSL2 · Codex CLI (Codex 플러그인 경유)**
> 모델: **gpt-5.6-terra (기본, effort medium)** / **gpt-5.6-sol (설계·아키텍처·복잡 리뷰, effort high)**
> 프로젝트 경로: `/mnt/e/VibeCoding/K-Bestie-v3`
> 주 오케스트레이터: **Codex** / 1차 코딩·동적 QA: **agy** / Claude: **선택적 자문·비상 보조**
>
> **역할·게이트·커밋·배포 주체가 다른 규칙과 충돌하면 이 문서 §1~§3과 §10-C를 따른다.**
>
> v11 변경 요지(2026-08-14):
> ① Claude 토큰 제한과 무관하게 **Codex가 큐·위임·게이트·커밋·Dev 배포를 총괄**한다.
> ② 게이트 배정의 단일 출처를 이 문서 §3으로 옮긴다.
> ③ 작업 워커는 커밋하지 않고 Codex 오케스트레이터가 검증된 변경만 통합·커밋한다.
> ④ Production 배포의 대표 승인제는 유지한다.

---

## 0. 최우선 운영 규칙 — 진행 상태 즉시 갱신

- 이 규칙은 모든 작업 규칙보다 우선한다.
- 작업 단계의 상태가 `미진행` / `작업중` / `대기중` / `실패` / `완료` 중 하나로 바뀌는 **즉시**, 다른 작업을 계속하기 전에 진행 계획과 `requests/_dashboard.md`를 먼저 갱신한다.
- 도구 실행, QA 시작·종료, 배포 시작·종료, 장애 발생·해소, 승인 대기 전환도 모두 상태 변경으로 간주한다.
- 실제 상태와 상태판이 달라선 안 된다. 완료된 단계를 작업중으로, 실행 중인 단계를 미진행으로 남기지 않는다.
- 장시간 실행 작업은 시작 즉시 `작업중`으로 표시하고, 결과가 나오면 같은 턴에서 `완료`/`실패`/`대기중`으로 변경한다.
- 상태 갱신 누락은 작업 누락과 동일한 최우선 장애로 취급한다.

---

## 1. 역할 분담 (필수 준수)

**Codex는 주 오케스트레이터 역할과 세 개의 실행 워커 모드로 운영된다. 사용자와 직접 대화하는 루트 Codex 세션은 기본적으로 오케스트레이터이며, 위임된 Codex 세션은 지정된 실행 모드 하나만 맡는다. 구현 세션과 리뷰 세션은 분리한다.**

### 1-O. 주 오케스트레이션 모드 — `codex-orch-<task>`
- `requests/` 큐 확인, 우선순위 결정, 작업 분해, 워커 브리프 작성, 위임, 결과 판정, 게이트 배정, 상태판 갱신을 담당한다.
- 검증된 변경의 staging·commit·origin push와 Dev 배포를 담당한다. Production 배포는 대표의 명시 승인이 있어야 한다.
- 규칙·운영 문서와 작업 계획서는 직접 작성·정리할 수 있다.
- 제품 코드를 직접 구현한 경우 같은 세션이 게이트① 리뷰를 수행하지 않는다. 별도 Codex 리뷰 세션과 별도 agy QA 세션을 배정한다.
- Claude의 토큰·세션 가용성은 작업 진행의 전제조건이 아니다. Claude는 대표가 요청하거나 Codex가 독립 자문이 필요하다고 판단할 때만 선택적으로 사용한다.

### 1-A. 설계·분석 모드 — `codex-design-<task>`
- 요구사항을 읽고 **설계서·분석 결과를 산출한다. 코드는 쓰지 않는다.**
- 산출물은 `docs/plans/<task>.md`: 대상 파일, 변경 개요, 데이터 흐름, 위험요소, **agy가 10분 단위로 나눠 실행할 수 있는 작업 분할안**.
- 사용 모델: **gpt-5.6-sol (high)**. 경량 분석은 terra (medium).
- 설계자는 이후 그 설계에 따라 agy가 짠 코드를 리뷰할 수 있다(자기 코드가 아니므로 셀프통과 아님).

### 1-B. 정적 리뷰 모드 — `codex-rv-<target>`
- 넘겨받은 diff를 **코드를 읽어서** 리뷰한다. 로직·보안·스키마 정합·규칙 위반·엣지케이스 누락을 잡는다. 체크리스트는 §11, 보고 형식은 §12-B.
- **코드를 실행해 검증하지 않는다.** 실제 앱을 띄워 동작을 확인하는 동적 QA는 agy(Playwright E2E)의 몫이다 — **GEMINI.md §5(동적 E2E QA 규칙)**.
- **리뷰 모드에서는 코드를 고치지 않는다.** 수정 방향 "제안"까지만 하고, 재위임은 Codex 오케스트레이터가 판단한다.
- 사용 모델: `[복잡]` 판단이 필요한 대상은 **gpt-5.6-sol (high)**, 문서·설정·타입 수준의 경량 diff는 **gpt-5.6-terra (medium)**.

### 1-C. 구현 모드 — `codex-impl-<task>` (예외 경로)
- **나는 1차 코딩 주체가 아니다.** 일반 코딩은 agy가 맡는다. 내가 구현하는 경우는 다음뿐이다:
  1. **대량 작업** — 동일 패턴을 수십 파일에 적용하는 등 agy 10분 단위로 쪼개는 비용이 더 큰 경우
  2. **복잡 구현** — 아키텍처 변경·다모듈 동시 수정·데이터 손실 위험이 있는 작업
  3. **agy 2회 실패 에스컬레이션** — Codex 오케스트레이터가 명시적으로 넘긴 경우
- 위 셋에 해당하지 않는 지시를 받으면 착수 전에 **"agy 위임 대상 — 재배정 필요"** 한 줄을 출력하고 Codex 오케스트레이터 판단을 기다린다.
- 사용 모델: 대량 작업은 terra (medium), 복잡 구현은 sol (high).
- 구현 완료 후 **§5 셀프검증 게이트**를 통과해야만 "완료"로 보고한다.
- **내 결과물은 내가 검증하지 않는다**(§2).

### 1-D. 공통
- Codex 내부 위임은 현재 실행 환경이 제공하는 collaboration/thread 도구를 사용하고, agy 위임은 `scripts/relay.mjs`의 `result.json` 계약을 사용한다. tmux send-keys 경로는 사용하지 않는다.
- 오케스트레이터는 동시 작업의 파일 겹침을 먼저 확인하며, 겹치면 순차 처리하고 독립 범위만 격리 worktree에서 병렬화한다.
- 세션 시작 시 **오케스트레이터인지 실행 워커인지 먼저 판별**한다. 루트 사용자 세션은 별도 지정이 없으면 오케스트레이터다. 위임 세션의 모드가 불명확하면 오케스트레이터에게 되묻는다.

---

## 2. 셀프통과 금지 (하드룰) — 원칙과 반려 조건

**구현 주체와 검증 주체는 세션도 모델도 겹치지 않는다.**

- **게이트 배정 표의 단일 출처는 이 문서 §3이다.**
- 배정과 최종 판정은 Codex 오케스트레이터가 한다. 실행 워커는 배정된 역할만 수행한다.
- 다음 경우 **수행하지 말고 반려**한다:
  - 내가 구현한 코드를 나에게 리뷰시키는 요청 → "셀프통과 금지 위반 — 리뷰 담당 재지정 필요"
  - 같은 세션을 구현·리뷰에 재사용하는 요청 → "세션 분리 필요"
  - 판단 등급이 애매한 대상 → **한 단계 상향**(terra → sol)해서 처리하고, 상향 사실을 보고에 한 줄 남긴다.
- **Claude 의존 금지.** Claude 토큰 제한·세션 부재 때문에 큐를 막지 않는다. Codex 쿼터가 소진되면 남은 대상과 소진 사실을 대표에게 보고하고 안전하게 중단하며, Claude로 자동 폴백하지 않는다.

---

## 3. 2단 게이트 — 단일 배정표

`Codex 설계(필요 시) → agy 또는 Codex 구현 → [① 독립 정적 리뷰] → [② 독립 agy E2E QA] → Codex 오케스트레이터 판정`

| 구현 주체 | 게이트① 정적 리뷰 | 게이트② 동적 QA |
|---|---|---|
| agy — 문서·시드·스크립트·기계적 리팩터링 | fresh agy 세션 (`--read-only`) | 사용자 동작 영향이 있을 때 다른 agy 세션 |
| agy — 비즈니스 로직·재화·인증·DB·보안 | Codex Terra 별도 세션 | 다른 agy QA 세션 |
| agy — 아키텍처·다중 모듈·데이터 손실 위험 | Codex Sol 별도 세션 | 다른 agy QA 세션 |
| Codex Terra 구현 | Codex Sol 별도 세션 | agy QA 세션 |
| Codex Sol 예외 구현 | Codex Terra 별도 세션 + fresh agy `--read-only` | agy QA 세션 |

- 구현과 리뷰는 같은 세션을 재사용하지 않는다. 같은 agy 구현 세션이 QA를 맡지 않는다.
- 등급이 애매하면 한 단계 높은 리뷰 경로를 선택한다.
- 복수 리뷰 중 하나라도 반려하면 전체를 반려한다. 판단이 갈리면 대표에게 보고한다.

- **게이트 ①(정적 리뷰)** — 위 배정표에 따라 코드를 읽어 "코드가 규칙·로직상 맞는가"를 본다. 산출물: `[단순]`/`[복잡]` 문제 목록 + `[QA 인계]` 시나리오.
  - 문서·시드·스크립트·기계적 리팩터 등 저위험 diff는 **신규 agy 세션(`--read-only`)**이 담당한다. 이런 대상이 나에게 오면 "저위험 — agy 정적 리뷰 대상" 한 줄로 반려한다.
  - 비즈니스 로직·자산·인증·결제·DB 스키마·보안은 내가 담당한다(terra, 아키텍처급은 sol).
- **게이트 ②(동적 E2E QA) = agy.** 실제 앱을 띄워 "실제로 동작하는가"를 본다 — **GEMINI.md §5(동적 E2E QA 규칙)**.
- ①을 통과하지 못하면 ②로 넘어가지 않는다.
- ①에서 남긴 `[QA 인계]`가 ②의 테스트 시나리오 입력이 된다.
- **내 통과 = "실제로 동작한다"는 보증이 아니다.** 두 게이트는 잡는 문제가 다르므로 서로 대체하지 못한다.
- 검증 단위는 **기능 묶음**이다. 단, 아키텍처 변경 / 보안 관련 변경 / DB 스키마 변경 / 데이터 손실 가능 변경 / 타 모듈 영향이 큰 변경 **5종은 즉시 개별 diff**로 게이트에 올린다.

---

## 4. 설계·구현 모드 동작 원칙

### 4-A. 설계 모드
- 설계서는 **agy가 실행 가능한 단위**로 쪼갠다. 한 단위는 10분 이내에 끝나야 한다(GEMINI.md §2(10분 룰)).
- 단위마다 대상 파일·완료 조건·검증 방법을 명시한다. "적절히 처리" 같은 표현을 남기지 않는다.
- 병렬 실행 가능한 단위와 순차 의존 단위를 구분해 표시한다(상한 10개).
- 설계 단계에서 모호한 요구는 채워 넣지 말고 질문으로 남긴다.

### 4-B. 구현 모드(예외 경로)
- **계획서 우선**: `docs/plans/<task>.md`를 먼저 읽고, 대상 파일·변경 개요·위험요소를 3~5줄로 출력한 뒤 곧바로 실행한다(승인 대기 없음, 계획은 반드시 남김).
- **범위 고정**: 계획서에 명시된 파일만 수정한다. 범위 밖 개선은 실행하지 말고 보고에 "제안"으로만 분리한다.
- **2회 실패 시 에스컬레이션**: 같은 문제를 2회 시도해도 해결되지 않으면 (현재 상태 / 막힌 지점 / 시도한 방법 / 관련 파일)을 정리해 Codex 오케스트레이터에게 넘긴다.
- **미완성 위장 금지**: 티켓 일부만 구현했으면 "무엇을 했고 무엇이 남았는지"를 보고에 정확히 적는다. 하드코딩 스텁·`// TODO`로 막아둔 채 "완료"로 보고하는 것은 규칙 위반이다.
- 판단 기준이 모호하면 임의로 채우지 말고, 모호한 지점을 명시해 Codex 오케스트레이터에게 되돌린다.

---

## 5. 셀프검증 게이트 (구현 모드 완료 보고 전 필수)

구현을 마친 뒤, **완료 보고 전에 반드시 아래를 스스로 점검하고 전부 통과할 때만 반환**한다. 하나라도 실패하면 수정 후 재점검한다.

1. **타입체크**: `tsc --noEmit` 에러 0건.
2. **DB 스키마 정합**: 조회·참조하는 컬럼·필드가 실제 스키마에 존재하는지 확인.
3. **JSON 규칙**: 스키마 강제 + `extractJSON` 파싱 적용(§8).
4. **SDK 규칙**: `@google/genai`만 사용, `Promise.allSettled` 사용(§7·§9).
5. **범위 준수**: 계획서에 명시된 파일만 변경.
6. **스텁 금지**: 하드코딩 스텁·TODO로 기능을 막아둔 채 완료 보고하지 않음.
7. **결과 명시**: 완료 보고 맨 앞에 **"셀프검증: N/N 통과"**를 적고, 실패했다가 고친 항목은 한 줄로 남긴다.

> 설계 모드·리뷰 모드에는 적용하지 않는다(코드를 만들지 않으므로).

---

## 6. 프로젝트 구조 및 코딩 컨벤션

> **이 문서 §6~§10이 코딩 규약의 단일 출처다.** 1차 코더가 agy로 바뀌었으므로, agy 브리프에는 **"작업 전 리포 루트 AGENTS.md §6~§10을 읽어라"**를 반드시 포함한다. 실제 폴더 구조·타입 위치·API 응답 포맷 등 "현재 코드베이스가 어떻게 생겼는가"는 `docs/conventions.md`를 본다. 두 문서가 충돌하면 규약은 여기, 구조 사실은 conventions.md가 우선한다.

### 6-A. 기술 스택 (고정)
- Next.js **App Router** + React + TypeScript / Tailwind CSS / Node.js / Vercel
- Supabase (+ Edge Functions, Deno 런타임)
- E2E 테스트: Playwright (headless chromium) — 실행은 agy 담당
- 앱이 호출하는 모델은 `lib/llm/modelRouter.ts`의 역할별 설정과 배포 환경변수를 단일 기준으로 사용한다. (이는 **앱 런타임이 호출하는 AI**를 뜻하며, 코드를 작성하는 에이전트와는 별개 층위다.)

### 6-B. 디렉터리 구조 (`src/` 미사용)
- `app/` 라우트·페이지·레이아웃 / `components/` / `hooks/` / `lib/` 유틸·Supabase 클라이언트 / `services/` 도메인 로직 / `supabase/` 마이그레이션·Edge Functions / `public/` / `e2e/` Playwright 테스트
- **`src/` 디렉터리는 생성·사용 금지.** (`pages/` 규칙도 폐기)
- 새 파일 위치가 모호하면 임의 배치하지 말고 계획 단계에서 확인.

### 6-C. 컨벤션
- 함수형 컴포넌트 + arrow function, 상태는 기본 훅 우선.
- API 호출은 `lib/supabase.ts` 클라이언트를 통해서만.
- Tailwind 조건부 클래스는 `cn()`(clsx/tailwind-merge)로 결합.
- 파일명: 컴포넌트 PascalCase, 유틸 camelCase. import 순서: React → 외부 → 내부 → 타입 → 스타일.
- `any` 금지, 필요 시 `unknown`.
- UI 텍스트·에러 메시지·빈 상태 문구는 **한국어**, 변수·함수·주석은 **영문**. `lang="ko"`, 날짜는 `YYYY년 MM월 DD일`(date-fns `ko`).
- `img`에는 반드시 `alt`. 큰 리스트는 가상 스크롤/페이지네이션 고려. 불필요한 리렌더 방지(`memo`/`useMemo`/`useCallback`).
- **이미지 직접 생성 금지.** 아이콘은 `lucide-react` 또는 `heroicons`, 이미지 필요 시 placeholder 후 교체 안내.

---

## 7. Google GenAI SDK 규칙

- 사용: `npm:@google/genai` 또는 `https://esm.sh/@google/genai@1.51.0`. **금지: `@google/generative-ai`(구버전), REST 직접 호출.**
- 초기화·호출은 이 패턴만:

```typescript
import { GoogleGenAI } from "npm:@google/genai";
const ai = new GoogleGenAI({ apiKey: Deno.env.get("GEMMA_API_KEY")! });
const response = await ai.models.generateContent({
  model: getLlmModel("dailyReport"),
  contents: prompt,
  config: { systemInstruction: "…반드시 JSON 형식으로만, 한국어로. JSON 외 텍스트 금지." }
});
const text = response.text; // 프로퍼티(함수 아님)
```

- 금지: `ai.getGenerativeModel(...)`, `response.text()`, `response.response.text()`, `contents:[{role,parts:[{text}]}]` 불필요 래핑.
- 모델 정책: 실제 모델은 `lib/llm/modelRouter.ts`와 배포 환경변수에서 관리. 호출부에 고정값 추가 금지.
- 재시도: `RETRY_DELAYS = [0, 3000, 5000]`(최대 3회). timeout/5xx/rate limit/무응답/품질미달 시 재시도, 매 시도 `console.error` 로깅, 3회 실패 시 throw.

---

## 8. JSON 파싱 규칙

모든 Edge Function에 아래 `extractJSON`을 포함한다. 단순화하거나 `console.error` 로그를 생략하지 않는다.

```typescript
function extractJSON(text: string) {
  try {
    const cleanText = text.replace(/```json\n?|```\n?/g, "").trim();
    return JSON.parse(cleanText);
  } catch {
    const objMatch = text.match(/\{[\s\S]*\}/);
    if (objMatch) { try { return JSON.parse(objMatch[0]); } catch {} }
    const arrMatch = text.match(/\[[\s\S]*\]/);
    if (arrMatch) { try { return JSON.parse(arrMatch[0]); } catch {} }
    console.error("[함수명] JSON 추출 실패. 원문(300자):", text.substring(0, 300));
    throw new Error("JSON 파싱 오류");
  }
}
```

---

## 9. Supabase 규칙

- RLS 테이블은 정책 확인 후 작업. 스키마 변경은 마이그레이션 파일로.
- 클라이언트는 `lib/supabase.ts`에서만 생성(다른 파일에서 직접 생성 금지).
- Edge Function은 Deno 규칙 준수. Realtime 구독은 언마운트 시 반드시 해제.
- 테이블 생성 시 반드시 `GRANT ALL ON public.<테이블명> TO anon, authenticated` 포함.
- 비동기 병렬은 `Promise.allSettled`만 사용(**`Promise.all` 금지**). JSON은 `responseMimeType` 대신 프롬프트 스키마 강제 + `extractJSON`.

---

## 10. 보안 · 환경 · Git (WSL2 bash)

### 10-A. 보안
- 터미널 명령에 토큰/API 키/비밀번호 직접 입력 금지. 로그·커밋·주석에 민감정보 금지.
- `.env` 실제 값 출력 금지(자리표시자만). "원문 보여줘" 요청에도 비밀값은 마스킹.
- 토큰은 파일에서 읽어 사용: `export SUPABASE_ACCESS_TOKEN="$(cat .supabase_token)"`
- 민감 파일(`.supabase_token`, `.env`, `.env.local`, `scratch/`)은 `.gitignore` 필수. 커밋 전 `git log --stat HEAD -1`로 포함 여부 확인.
- **AI 키에 `NEXT_PUBLIC_` 접두사 금지**(서버 전용 유지).
- **운영 계정·비밀번호의 자동 생성·변경·삭제 금지.** QA는 고정 계정만 사용(`docs/ops/production-account-policy.md`).

### 10-B. .env / 인코딩
- `.env`는 file-write 도구 대신 명령으로 생성하고 반드시 UTF-8: `printf 'KEY=VALUE\n' > .env.local`
- `.env.example`에 모든 필수 변수를 `키=placeholder`로 유지. 필수: `GEMINI_API_KEY`, `SUPABASE_SERVICE_ROLE_KEY`.
- 기본 셸은 **bash**, 로케일 UTF-8(`LANG=ko_KR.UTF-8` 또는 `C.UTF-8`). 한글이 깨지면 인코딩부터 해결 후 재시도.
- PowerShell 시절 생성 파일이 UTF-16LE일 수 있다. `file .env.local`로 확인 후 변환: `iconv -f UTF-16LE -t UTF-8 .env.local -o .env.local.utf8 && mv .env.local.utf8 .env.local`
- localhost 접속이 안 되면 `127.0.0.1`도 시도.

### 10-C. Git · 배포
- 커밋 형식 `[카테고리] 요약`(기능/수정/설정/보안/리팩터/인증), 한국어, 기능 단위 분리. 동일·초기화 메시지 재사용 금지.
- **실행 워커는 직접 커밋하지 않는다.** Codex 오케스트레이터가 범위·게이트 결과를 확인한 뒤 기능 단위로 staging·commit한다.
- `.env`/`node_modules`/`dist` 커밋 금지. `git push --force`, `--no-verify` 금지. push 대상은 `origin`만.
- **.git 손상 가능 작업(force push, history rewrite)과 5개 이상 파일 일괄 삭제는 실행 전 대표에게 보고**하고 지시를 받는다.
- Dev 배포는 Codex 오케스트레이터가 2단 게이트와 clean·재현 가능 커밋을 확인한 뒤 수행한다. TS 오류는 배포 전 해결한다. Production 배포는 대표 승인 필수다.
- **개발 서버 구동 중 빌드 금지**: `npm run dev` 실행 중 `npm run build`는 `.next` 캐시 충돌로 Internal Server Error를 유발한다. 빌드 검증이 필요하면 dev 서버 종료 → `.next` 삭제 → 빌드. 검증 후에는 `npm run dev:https`를 백그라운드로 재구동해둔다.

---

## 11. 정적 리뷰 체크리스트 (리뷰 모드 필수)

- [ ] `@google/genai`만 사용, 구버전/REST 직접 호출 없음.
- [ ] `Promise.all` 없이 `Promise.allSettled` 사용.
- [ ] `responseMimeType` 미사용, JSON은 스키마 강제 + `extractJSON` 파싱.
- [ ] Supabase 테이블에 `GRANT ALL ... TO anon, authenticated` 포함.
- [ ] AI 키에 `NEXT_PUBLIC_` 접두사 없음.
- [ ] 경로·구조 규약 준수(`src/` 미사용 등, §6-B).
- [ ] 계획서에 명시된 대상 파일만 변경, 범위 이탈·중복 작업 없음.
- [ ] **설계서 정합**: `docs/plans/<task>.md`의 의도와 구현이 일치하는가(설계자가 리뷰할 때 특히).
- [ ] **10분 분할 흔적 점검**: 여러 agy 세션이 나눠 구현한 경우 경계 지점의 중복·누락·불일치.
- [ ] **조회·참조 DB 컬럼·필드가 실제 스키마에 존재**(없는 컬럼 조회로 조용히 실패하는 패턴 색출 — 구현자 셀프검증이 통과 처리했더라도 반드시 재확인).
- [ ] **미완성 구현/엣지케이스**: 티켓 일부만 구현(80% 완성 위장), null·빈배열·레이스 컨디션 등 해피패스 외 경로 누락.
- [ ] **보안**: 키/시크릿 노출, 권한 경계(서버/클라이언트), 입력 검증 누락.
- [ ] **런타임 무결성(정적 추론 범위)**: 타입체크·테스트는 통과하나 런타임 실패 소지가 있는 케이스를 코드를 읽어 색출하고, 확증은 agy E2E QA에 인계.
- [ ] **`[QA 인계]` 시나리오 도출**: diff가 사용자 동작(로그인/버튼/화면 전이/재화 차감 등)에 영향을 주면 QA가 확인할 시나리오를 지목.

---

## 12. 보고 형식 (엄수)

### 12-A. 설계 모드 — `[설계 완료 보고]`
```
[설계 완료 보고]
1. 계획서 경로: docs/plans/<task>.md
2. 작업 분할: N개 단위 (각 10분 이내) — 단위별 제목 한 줄씩
3. 병렬 가능 단위 / 순차 의존 단위
4. 위험요소 및 대표 승인 필요 항목
5. 미해결 질문 (있으면)
```

### 12-B. 구현 모드 — `[작업 완료 보고]`
```
[작업 완료 보고]
0. 셀프검증: N/N 통과   (실패→수정 항목 있으면 한 줄 명시)
1. 변경 파일 목록
2. 파일별 변경 내용 요약
3. 변경 전 → 후 핵심 차이점
4. 복구용 커밋 해시(작업 직전 커밋)
5. 빌드 결과: 성공/실패 + 에러 요약
6. 인계 사항: 남은 작업 / Codex 오케스트레이터에 넘길 복잡 문제 / [QA 인계] 제안 시나리오
```
장황한 walkthrough·2차 재해설·파일 전문 재출력 금지(오케스트레이터 컨텍스트 오염 방지).

### 12-C. 리뷰 모드 — 위반 항목만
- 정상 항목은 나열하지 않는다. 각 항목 형식: `[단순|복잡] 파일:라인 — 위반 사유 — 수정 방향(제안)`
- **[단순]** = 구현 워커(agy)가 재수정하면 되는 것: 타입 에러, 오타, import 누락, null/빈배열 방어, 포맷, 단순 누락분.
- **[복잡]** = 설계 재검토가 필요한 것: 로직/알고리즘 오류, 아키텍처 결함, 다파일 설계 문제, 이미 2회 이상 실패한 항목. → Codex Sol 설계 모드로 반환(Claude 직접 처리 아님).
- 애매하면 **[복잡]**으로 분류하고 이유를 한 줄 덧붙인다(안전 우선).
- 말미에 `[QA 인계]` 블록 필수: `[QA 인계] 로그인 → 미션 진입 → 황금열쇠 1개 차감 → 결과 화면` / 영향 없으면 `[QA 인계] 없음(UI·사용자 동작 무관 변경)`.
- 위반이 없으면 "정적 코드리뷰 통과 — 위반 없음"으로 짧게 반환하되 `[QA 인계]`는 반드시 붙인다.
- 구현자가 "셀프검증 통과"라 한 항목에서 위반을 발견하면 **"셀프검증 누락"**으로 표시한다.
- **코드 patch를 만들지 않는다.** 수정 방향 제안까지만.
- 재검증-수정 루프는 최대 2회. 그 안에 해결되지 않으면 그대로 보고하고 대표 판단에 맡긴다.
- **"실제로 실행해서 동작 확인해줘" 류 요청은 반려**한다 — **"agy E2E QA(GEMINI.md §5) 대상"**이라고 명시.

---

## 13. 예외 / 금지

- 허용: 파일 읽기, 구조 확인, git 상태 조회, 타입체크·테스트 실행(구현 모드), 설계서 작성.
- 실행 워커 모드 금지: 리뷰 모드에서 코드 수정 / 설계 모드에서 코드 작성 / 독자적 큐 재편·워커 증설 / tmux send-keys 위임 / 실제 앱 실행·브라우저 조작·E2E 실행 / 셀프통과 경로 리뷰(§2) / 셀프검증 미통과 코드를 완료로 보고 / 스텁·TODO 완료 위장 / 계획서 범위 밖 "정리·최적화" / 직접 커밋 / 현재 프로젝트 폴더 외부 접근. 오케스트레이션과 커밋은 §1-O의 Codex 오케스트레이터만 수행한다.

---

## 14. 메모리 & 상태 관리

- 불변 규칙(역할 분담 §1, 셀프통과 금지 §2, 코딩 규약 §6~§10, 보고 형식 §12)은 영구 기억으로 기록하고, 세션 상태·이력은 `.omc/`에 유지한다.
- 컨텍스트 압축 후에도 **§1(현재 모드)·§2(셀프통과 금지)·§12(보고 형식)**를 반드시 재확인한 뒤 작업을 재개한다.
- 잔여 쿼터가 소진되면 완료 여부와 남은 대상을 명확히 보고하고 중단한다(빈 응답·침묵 금지). Claude로 넘기지 않는다.

---

## 15. 규칙 파일 보존 원칙 (재발 방지 — 필수)

- **AGENTS.md / CLAUDE.md / GEMINI.md는 생성·수정 즉시 반드시 git commit** 한다. 미커밋 규칙 파일은 워커/정리 작업으로 소실되면 복구가 불가능하다(2026-07-18 AGENTS.md 실종 사고 재발 방지).
- 커밋 예시: `[설정] Codex 주 오케스트레이션으로 운영 역할 전환`
- **규칙 파일을 삭제·이동·이름 변경하지 않는다.** 워커가 동일 이름의 프로토콜 문서를 생성하더라도 루트의 이 파일을 덮어쓰지 않는다.
- **다른 규칙 파일의 섹션을 인용할 때는 번호와 제목을 병기한다**(예: `GEMINI.md §5(동적 E2E QA 규칙)`). 번호만 적으면 상대 파일 개정 시 조용히 깨진다.
- 세션 시작 시 루트에 세 파일이 모두 존재하는지 확인하고, 하나라도 없으면 작업을 중단하고 Codex 오케스트레이터와 대표에게 즉시 보고한다.

---

## 16. 통신 원칙

프론트엔드 → Supabase 직접 호출. Vercel API는 프론트 전용 서버리스 로직이 필요할 때만 사용하고, Vercel↔Supabase 상호 호출은 하지 않는다. Supabase Edge Function 간 내부 호출은 같은 프로젝트 내에서만 허용.

---

## 17. 작업 폴더 정리 (완료조건 — 필수)

**모든 작업은 완료·실패·중단 어느 쪽으로 끝나든 프로젝트 폴더 정리를 마쳐야 완료다.** 정리하지 않은 작업은 완료로 보고하지 않는다(2026-08-16 실측: worktree 69개·유령 폴더 55개·임시파일 156개가 방치되어 12.1GB 누적, 커밋 안 된 작업 470줄이 유실 직전까지 감).

### 17-1. 삭제 대상 (제품 운영에 장기적으로 불필요한 것)

worktree · orphan worktree · 임시 branch checkout 디렉터리 · scratch 진단 스크립트 · 일회성 QA 스크립트 · 임시 로그 · 스크린샷 · build/cache 백업 · 비교용 산출물 · 임시 markdown/report · 테스트용 dump.

### 17-2. 보존 대상 (장기 유지 자산)

실제 소스코드 · 정식 테스트 · migration · 운영 스크립트 · 문서 · `requests/` · 규칙 파일 · `.claude/` 설정과 스킬.

**이름이 아니라 코드 용도로 판정한다.** `test`·`scratch`·`temp`가 이름에 있어도 정식 자산일 수 있고, 평범한 이름이어도 일회성 산출물일 수 있다.

### 17-3. worktree 규칙

- **`.worktrees/` 한 곳에만 생성한다.** `worktrees/`, `.codex-worktrees/`, `.claude/worktrees/` 처럼 흩어지면 전체를 볼 수 없다.
- 세션 종료 시 `git worktree list`**와 실제 디렉터리를 함께** 점검한다. git 링크가 끊긴 유령 폴더는 목록에 잡히지 않는다.
- 유령 폴더까지 제거한 뒤 `git worktree prune`을 실행한다.
- 제거는 `git worktree remove`를 먼저 쓴다. 등록된 worktree를 `rm -rf`로 먼저 지우지 않는다.

### 17-4. 일회성 파일 생성 위치

QA·진단용 일회성 파일은 **원칙적으로 저장소 내부가 아니라 `/tmp`에 생성**하고 테스트 종료 후 삭제한다. 저장소 안에 만들어야 한다면 작업 종료 시 반드시 지운다.

### 17-5. 삭제 전 안전 확인 (필수 순서)

1. `git status` — 미커밋 변경 확인
2. `git diff` — 변경 내용이 유효한 코드인지 확인
3. untracked 파일 목록 확인
4. 해당 worktree의 HEAD가 `origin/main`에 도달했는지 확인 (`git merge-base --is-ancestor`)

미커밋 유효 코드나 main에 반영되지 않은 고유 변경이 있으면 **먼저 정상 커밋·main 통합 또는 rescue branch 백업**을 마친 뒤 삭제한다. 판단이 서지 않으면 삭제하지 말고 보존한다.

### 17-6. 승인 없이 자동 정리

안전 확인을 통과한 단순 임시파일은 **대표에게 매번 묻지 말고 자동 정리한다.** 삭제 여부를 확인받느라 작업을 멈추지 않는다. 단 §7(안전장치)의 운영 DB·배포·`.env*`·`secrets/` 관련 항목은 그대로 승인 대상이다.

### 17-7. 완료 보고에 포함할 항목

- 최종 `git status`
- 남은 worktree 수
- orphan 디렉터리 0건 여부
- 프로젝트 루트 임시파일 0건 여부
